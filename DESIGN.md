# PocketDesk · DESIGN.md

> 本文件是这个项目视觉系统的唯一权威来源。任何 agent 或人改动 `Web/` 之前先读这里；改完必须让它仍然成立。
> 由 2026-09-06 两次会话的实测数据整理（视口 390×844 / 320×568，Chrome + 真机 Safari 待复核）。
> 配套：`CLAUDE.md`（目录契约）、`Web/CLAUDE.md`（前端约定）、`README.md`（行为与安全边界）。

## 0. 为什么需要这份文件

这个仓库在同一天里被重写过两次，每次都没留下"为什么这样定"的依据，于是：`.targets` 的 scroll-snap 被后写的规则吃掉、消息行靠负 margin 补位、圆角同时存在 99 与 999 两种胶囊、配色从深海军蓝+薄荷绿整块换成紫罗兰渐变。**"不规范"的根因不是审美，是没有可核对的权威文件。** 这份文件就是那个可核对的东西。

## 1. 这个界面在为什么服务

- 模式：**Operate**（用户要完成任务，不是被说服）。手机是遥控器，Mac 是被操作端。
- 使用场景：单手握持、可能是走路或躺床上、屏幕可能很亮、目标只有一个动作——把一段话说进某个桌面应用。
- 成功判据：① 一眼确认"发给谁"；② 一次误触都不会毁掉已有内容；③ 出错时知道下一步做什么。
- 优先级顺序冲突时按此裁决：**目标不出错 > 输入不丢失 > 状态可读 > 手感愉悦 > 视觉华丽**。

## 2. 令牌（唯一取值处：`Web/style.css` 的 `:root`）

规则：`:root` 之外不允许出现色值字面量；除边框宽度与图标尺寸外不允许出现 px 字面量。新增视觉需求先加令牌，再引用令牌。

**本文件不抄录具体色值。** 一天之内这个界面换了三套皮肤（深海军蓝+薄荷绿 → 紫罗兰深色 → 浅灰蓝玻璃+蓝强调），把值写进文档只会让文档天天说谎。令牌的名字、角色与不变量才是契约；值以 `Web/style.css` 的 `:root` 为唯一真相。

| 组 | 必需令牌 | 角色与不变量 |
|---|---|---|
| 背景 | `--bg` `--surface-1..3` `--field` | 表面最多三级 + 一个输入底；`--surface-*` 可以是半透玻璃，但必须与 `--bg` 拉开 ≥ 1.2:1 以便分层可读 |
| 描边 | `--border` `--border-strong` | 1px 常规、选中态 ≥ 1.5px；浅色主题下 `--border-strong` 对 `--bg` 不得低于 3:1，否则边框等于没有 |
| 文本 | `--fg` `--fg-muted` `--fg-faint` | 必读信息一律 `--fg`/`--fg-muted`（≥ 4.5:1，正文目标 7:1）；`--fg-faint` 只给装饰与次要注记，不得用于提示语、错误语、占位符 |
| 强调 | `--accent` `--accent-ink` `--accent-soft` `--accent-line` | **全局只允许一个强调色**；`--accent-ink` 是落在 `--accent` 上的文字色，两者对比必须 ≥ 4.5:1（实测 4.56 是下限，不得再降） |
| 语义 | `--danger` | 失败与破坏性动作，对实际叠底色 ≥ 4.5:1 |
| 高光 | `--hairline-top` `--inset-top` | 只允许 1px 内描边/`inset` 阴影表达玻璃边缘；不得用外发光代替 |
| 光效 | `--halo-*` `--glow-*` `--lift-*` | 待裁决项，见 §3.1：允许存在但必须集中在 `:root`，且不得参与文字背底 |
| 间距 | `--s-1..--s-8` | 4px 基准刻度，禁止表外间距与负 margin |
| 圆角 | `--r-field` `--r-icon` `--r-panel` `--r-pill` | 四档，禁止第五档 |
| 字号 | `--text-xs..xl` | 五档；`textarea` 必须 16px（否则 iOS 聚焦缩放） |
| 动效 | `--dur` `--ease` | 只动 `transform` 与 `opacity`（`filter`、`box-shadow` 不参与过渡） |
| 尺寸 | `--tile-w` `--icon-size` | tile 实测 68×86；所有可点控件最细一边 ≥ 44px |

已修复项的实测前后（保留作为回归基线，390×844 / 320×568）：

| 项 | 修前 | 修后 |
|---|---|---|
| `.pad-hint` 手势说明 | 3.16:1 | **7.70** |
| `#send` 按钮文字 | 2.72–4.44:1（白字压渐变） | **7.12**（深主题）/ **4.56**（浅主题 `#1f6bff` + 白） |
| `textarea::placeholder` | 4.11:1 | **7.64** |
| 错误文案 | 4.46:1 | **5.06** |
| 选中态信号 | 底色差 1.06–1.3:1（不可见） | 1.5px 实描边 + 名称加粗，≥ 2 重 |
| `#wake` / 滑杆命中区 | 50×26.5 / 129×16 | 50×**44** / 118×**44** |
| `.pad-tuning` 溢出 | 超卡片右继 7px | 越界数 **0**（两档规均无横向溢出） |
| `:root` 外硬编码色值 | 9 处 | **0 处** |
| Tab 停靠点 | 6 个 tile 全部可 Tab | roving tabindex：选中 tile → 唤醒 → pad → 两滑杆 → 文本框 → 发送 |

## 3. 禁令（会被 reviewer 直接打回的）

1. **单一强调色 + 渐变按钮 + 外发光 + 页底饱和径向光晕**这套"AI 默认高级感"组合是被禁的。**当前仍有 5 个光效令牌在用**（`--halo-pad` `--lift-tile` `--glow-dot` `--glow-hint` `--glow-thumb`）——它们已被收进 `:root`（符合 §3.3），但光效本身与禁令相冲。要么保留禁令、把 5 个令牌降为极弱值或删除；要么正式修订本文件允许光晕。**此项需人拍板，不得由 agent 顺手改**（见 §6.1）。
2 禁止 emoji 充当图标。
3. 禁止在 `:root` 外写色值、禁止一行压多条规则（可 diff 是硬要求）。
4. 禁止用后写的规则覆盖前面的规则来"打补丁"——改就改原处（`.targets` 的 scroll-snap 事故）。
5. 禁止负 margin 补间距——用容器的 `gap`。
6. 禁止把两套页面写成两套样式系统（`console.html` 现在自带一份，必须与 `Web/style.css` 共用令牌）。
7. 禁止 `transition: all`；禁止动 `top/left/width/height`。

## 4. 组件契约

| 组件 | 状态 | 必须做到 |
|---|---|---|
| `.target` Dock 图标 | default / selected / active / dragging / unavailable | 选中=描边+名称变亮（≥2 重信号）；未安装必须可见地灰化；`active` 有 0.96 下压 |
| `#pad` 触控板 | idle / active / expanded / 通道断开 | 通道断开时给出可见状态点，不能静默失败 |
| `#send` | idle / 三阶段进行中 / disabled / 失败 | 文案必须带目标应用名；进行中显示阶段；失败可原地重试 |
| `#message` | info / error | 成功态自动消退；错误态不自动消退且给出下一步 |
| `.pill` 连接态 | 连接中 / 已就绪 / 需授权 / 未连接 | 颜色语义必须跟文案一致，禁止"需授权"却显示就绪绿 |

## 5. 图标策略

- **PocketDesk 品牌图标**：基于第一版蓝紫底板与白色 P 口袋，P 下端收口融合鼠标指针轮廓，内部保留一块简化显示器及底座，明确桌面控制含义；不放多个微型应用图标，只保留一道口袋翻折，优先保证小尺寸的整体轮廓。原稿为 `Resources/icon-source.png`，用 `scripts/import-icon.swift` 生成带透明边距的 iconset，再由 iconutil 打包。此约定只覆盖品牌资产，不改变网页配色。

- **目标应用**：一律系统真实图标（`/api/icon`）。请求尺寸 256、`object-fit: contain` + 2px 内衬（`cover` 会裁掉 macOS 图标自带安全边距，导致视觉重量不一）；缓存 key 带上 app bundle mtime，避免更新后图标不刷新。取不到时用统一占位符号，不要中英混排的首字母。
- **界面控件**：一律内联 `<symbol>` sprite，24×24 viewBox、`stroke-width: 1.75`、`stroke: currentColor`、`fill: none`（只有点阵类符号允许 fill）。不引第三方图标库、不用 CDN——页面必须能在无外网的局域网里跑。
- 待建：`Web/` 目前**没有任何 sprite 符号**，本节是要求而不是现状。需要新增的符号：`i-grip`（排序把手）、`i-check`（完成）、`i-reset`（恢复默认）、`i-mic`、`i-spinner`、`i-alert`、`i-wifi-off`、`i-app-missing`。
- **图标体积必须与缓存策略一起改**：`/api/icon` 现在按 256px 输出、单张 ~32KB、且 `Cache-Control: no-store` → 7 个目标 = 每次进页 **224KB**。改小不如改成可缓存：带 ETag/`max-age` + 缓存 key 上 bundle mtime。

## 6. 待决事项（需要人拍板，不要由 agent 顺手改）

1. **配色与光效方向（已经换了三次，这是本项最严重的规范问题）**：12:00 深海军蓝 + 薄荷绿 → 15:1x 紫罗兰深色 + 渐变/外发光 → 17:3x 浅灰蓝玻璃 + `#1f6bff` 蓝强调。每次都是“皮肤整体重写”，而层级、令牌名、组件契约一直沿用，所以代码质看像在变好、产品牌看像在反复推倒。当前遗留：`#send` 白字对 `#1f6bff` 只有 **4.56:1**（刚过 AA 下限，阳光下偏弱），以及 §3.1 与 5 个光效令牌的正面冲突。**请定一个世界并写回本文件：浅色玻璃（建议 `--accent` 压深到 `#1a5ae0` 级别换回余量）/ 深色薄荷绿 / 深色紫，并明确光晕允许到哪个程度。**
2. **顺序与目标管理的归属**：手机拖动排序 vs 电脑端控制台。当前实现里手机侧拖动已消失；已验证过的引擎在 `~/.cola/coding-cola/project-.../dock-drag.js` + `dock-drag.css`，可并入任一侧。
3. **是否公开**：`/api/send` 与新的 ws:46388 触控板通道目前**无鉴权**，公开仓库会招来"局域网 keystroke 注入"这类安全议题；公开前建议至少在 README 顶部保留警告，并考虑加配对 token。
