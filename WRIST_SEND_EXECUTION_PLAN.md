# PocketDesk 翻腕发送与统一手机设置执行方案

> 2026-09-10 修订（基线由 `278ac69` 上移至 `f0ff85e`，手机资源版本由 `2.9.23` 变为 style `3.0.10` / app.js·compose.js `3.0.22` / screen `3.1.0` / unlock `1`）。这是交接给执行模型的方案，尚未实现、尚未通过真机验证。执行前重新核对工作树，保留用户已有修改。
> 2026-09-11 状态：**已实现并部署**（motion-recognizer.js 纯识别器 + motion-send.js 传感器侧 + 统一手机设置），纯函数单测 8/8 通过；**真机（iPhone / vivo）手感与灵敏度标定仍待用户实测**。同日锁屏解锁功能已移除（从未通过真机验收）。
> 用户目标：说完话后用一个小幅手腕动作发送；必须考虑 iPhone「摇动以撤销」；页面简洁清晰，设置只有一个入口。本轮只编写方案，不修改运行代码。
> 2026-09-11 补充（免证书降级）：**第 5 节的「手机信任 CA」与第 9 节第 2 步的强制安装要求已作废**，改按 `docs/wrist-send-no-certificate-plan.md` 执行——翻腕不得要求手机下载、安装或手动信任 CA；非安全上下文整组隐藏，运动权限并入用户开启的那次点按，能力以真实有效传感器数据证明（静止的合法零值不算失败）。本文件其余部分（面板结构、识别器、发送门禁、iOS/Android 双轨真机门槛）仍然有效。
> 已定前提：iPhone 与 Android **双轨**验收（用户两台都用，Android 主力为 vivo + 豆包输入法）；安全上下文复用既有原生 HTTPS `:46487`，**不另搭反代**。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md

## 1. 产品决策与范围

- 名称用「翻腕发送」，不要用「摇一摇发送」。候选动作：手机上沿向远离自己的方向轻翻一下，停住片刻，随后自然回位。动作可行性必须由真机决定，不预先承诺与系统撤销或系统手势完全隔离（iOS 与 Android 分别判定）。
- 默认关闭；启用、练习、权限与故障说明全部放入同一个「手机设置」面板。输入页、触控板、全屏 HUD 不各自新增齿轮。
- 复用已有发送语义：文字、图片、实时同频提交、历史、错误反馈走同一发送入口。不得单独向 Mac 裸发 Return，也不得模拟 DOM 点击绕过统一发送门禁。
- 保留既有按钮和键盘发送。设置整合可以独立交付，体感功能必须经过设备能力、系统冲突和发送一致性三道验证。
- 本次不做空中鼠标、原生手机 App、自动识别系统听写结束、云账号设置同步、更多手势或主题重设计，也不搭 HTTPS 反代（复用既有原生安全通道）。

## 2. 已核对的现状与需要注意的差异

| 位置 | 当前事实 | 执行影响 |
|---|---|---|
| `Web/index.html` | header 为标题 + `#pad-target` + `#connection`；`#pad-settings` 仍在 `#pad` 内（第 40 行） | 设置迁出远端操控区，放到全局 header |
| `Web/app.js` | `exitPadMode()`（197–202）移除 `show-tuning` 并复位 `padSettings` 的 `aria-expanded` | 移除旧耦合，不能删 DOM 后留下空引用 |
| `Web/pad.js` | 齿轮开关键在 177 行 `padCard.classList.toggle('show-tuning')` | 与 `exitPadMode()` 一并改，避免两处各留一半状态 |
| `Web/app-extras.css` | `.pad-tuning` / `.pad-gear` / `show-tuning` 的主规则实际在这里（10–57、353、429），**不在 style.css** | 改这里；不要去 `style.css` 找旧齿轮 |
| `Web/style.css` | 只保留 `.pad-tuning` 基础形态（624–692），后置与主题覆盖在 app-extras | 按层叠顺序判定，不末尾叠覆盖 |
| 触控板偏好 | `#sens` / `#scroll-speed` / `#ruler-mode`，键 `pd-sens` / `pd-scroll` / `pd-ruler`（pad.js 159–169） | 移动现有控件，保留 ID、范围、值与数据；不要复制第二份 |
| `send()` | compose.js:398；已有 `submittingDraft` 业务锁，**但没有返回值**（只在内部 `message()`） | 锁已够用；新增结构化返回（跳过/已提交/未确认/失败）供所有入口共用 |
| `pushLive()` | **已修好**：`compose-queue.js` 的 `ComposeQueue.push()` 为每个 item 独立 resolve waiter，且 submit 不参与合并 | 不要再当缺陷修。改测第 7 节列出的「前序失败连带拒绝在途 submit」 |
| `sendFromKeyboard()` | compose.js:356 仍是 `await send(); if (!textEl.value) hideKeyboard();` | 改为依据提交结果，避免把空草稿、失败或异步更新误认为成功 |
| `wsEndpoint()` | pad.js:16–19；按 `location.protocol` 切 ws/wss，默认端口 +1 推导 | HTTPS 走 46488：需确认非默认端口推导在 46487 入口下仍成立 |
| `Sources/Server.swift` | 静态白名单已是 13 项显式列表（第 460 行） | 新脚本加入白名单及 MIME，不扩大成任意文件读取 |
| HTTPS / WS 鉴权 | `SecureTransport`(:46487) 复用同一套 `route`/`serveFile`；WS(:46488) 与帧(:46489) 各自校验 token 或本地 peer；回环豁免（`Server.swift` 103–115）对 secure 监听同样生效 | 无反代即无转发头风险；回环豁免仅在文档点明，不作为远端绕过面 |

现有 DESIGN.md 的部分行为描述早于当前代码；执行以用户本方案决策和实读代码为准，保留既有颜色、字体、图标和令牌体系，不顺手重做旧设计争议。

## 3. 页面结构：一个入口，一层面板

### 3.1 主页面

将触控板旧齿轮迁移为顶部右侧唯一「手机设置」入口，建议 ID `#phone-settings`。触控板不再保留局部齿轮，也不保留原位滑杆。连接状态仍只表示连接，不承担设置或体感状态。

```text
PocketDesk        当前应用   连接状态   设置
应用 Dock
触控板（收起 / 展开保持现有行为）
输入区
历史 / 图片 / 电脑画面 / 发送
既有消息行
```

这里的「设置」是线框中的语义标签，实际使用既有线条齿轮，提供 `aria-label="手机设置"`、title 和至少 44×44 的触控区域。不要新增背景胶囊、常驻体感徽章、第二排工具条或醒目功能宣传卡。

宽度不足时 header 用稳定的两行 grid：第一行标题与设置，第二行当前应用与连接；应用名省略但可访问完整名称。不可为了塞下入口压缩字号或隐藏连接。避免 flex 任意换行造成齿轮跳位置。

### 3.2 手机设置面板

手机使用底部模态面板，大屏仍是同一份 DOM，限宽居中。标题与关闭按钮固定，内容可滚动，适配安全区；不使用嵌套弹窗、分页、tabs 或每组一个「更多设置」。

```text
手机设置                               关闭
仅影响这台手机

触控板
  光标速度          [原滑杆]
  滚动速度          [原滑杆]
  边缘刻度          [双排 / 单排]

翻腕发送                              [关闭]
  手机向前轻翻，停住片刻后发送。
  （按状态按需出现：授权说明、练习入口、不可用原因）
```

- 顺序固定：已有触控板在前，新功能在后；两个简洁分组，不做两张厚重卡片。
- 偏好即改即生效，不加「保存」「应用」按钮。关闭只负责关闭；第一版不新增全局恢复默认或高级选项。
- 功能关闭且未操作时只显示开关与一句解释。用户尝试开启后，才就地显示与当前失败原因相关的说明。
- 练习按钮是功能组内的操作，不是另一个设置入口。练习界面在本组内展开，包含「练习中，不会发送」与「完成练习」；无实时数据仪表盘。
- 不暴露加速度、角度、冷却等参数滑杆；研发参数集中在代码配置，真机验证后定稿。
- 手动点击开启需要授权时，在该次点击处理里直接请求，不要经过网络等待后才调用权限 API。

### 3.3 操作与设置的边界

| 内容 | 归属 |
|---|---|
| 光标速度、滚动速度、刻度、翻腕开关与练习 | 手机设置，当前浏览器 origin 本地偏好 |
| 目标管理、应用专属快捷键、默认面板、产品主题、连接配置 | 继续由电脑控制台负责，不在手机再造一份 |
| 发送、图片、历史、画面入口 | 主页面即时操作，保持原位置 |
| 全屏右键、滚动模式、键盘、刷新、退出 | 全屏即时操作，保持现状，不能误当设置收走 |

全屏不新增设置齿轮：需要调整时先用既有退出全屏操作回主页，再打开设置。已经启用的翻腕可在通过测试的全屏场景运行。

### 3.4 焦点、层级与关闭

- 建议原生 `dialog.showModal()`；以 dialog.open 为真值源，使用 aria-labelledby、焦点限制、Esc/关闭、可靠的遮罩点击判断及关闭后焦点恢复。
- 设置开启前暂停真实体感发送，收起主页或全屏代理键盘，保存草稿与目标；关闭回到齿轮，不自动弹键盘。不改变 pad-mode。
- 面板内操作不向 Mac 发点击、滚动或快捷键。遮罩关闭只消费本次指针序列，不穿透点击下面触控板。
- PiP 开着时设置在它上方，关闭后保留 PiP 位置、尺寸、显示器；原生全屏状态先经既有退出流程退出，不创建两层原生全屏/模态冲突。
- 全屏态已存在 `<dialog id="screen-view">` 与 `#screen-menu`（画面与操作设置，index.html 119–128）。`#phone-settings` 与它们是同层 dialog：全屏态下手机设置默认不可用，先走既有退出全屏回主页再打开；确需在全屏打开时，必须先收起 `#screen-menu`，任一时刻只允许一个 dialog 为 open，禁止 `showModal()` 嵌套叠加。
- 练习是唯一允许设置打开时读取体感的模式，识别结果绝不接入真实发送；退出、关闭、页面隐藏均结束练习并丢弃候选动作。
- VoiceOver 标签完整、Tab 顺序合理、两套主题可读、减少动态效果偏好生效。翻腕是可选捷径，不能替代可访问的发送方式。

## 4. 系统撤销 / 手势冲突：不可省略的上线条件（iOS + Android 双轨）

### 4.1 平台事实

`devicemotion` 不可取消，网页不能用 preventDefault 拦住系统摇动撤销；网页也没有可依赖的 API 查询用户「摇动以撤销」开关或可靠得知系统撤销提示框何时出现。不要用 blur、visibilitychange 或 beforeinput 冒充系统提示检测。[MDN](https://developer.mozilla.org/en-US/docs/Web/API/Window/devicemotion_event)

iPhone 可由用户在「设置 → 辅助功能 → 触控」关闭「摇动以撤销」，这是全局偏好；苹果还提供三指左滑撤销文字的方式。不要自动跳转未经支持的私有设置 URL，也不要声称已替用户关闭。[Apple 支持](https://support.apple.com/en-lamr/guide/iphone/iph77bcdd132/ios)

原生 UIKit 的 applicationSupportsShakeToEdit 可控制本 App 的摇动撤销界面，但当前手机网页无此权限。安装到主屏幕不等于获得 UIKit API。[Apple 开发文档](https://developer.apple.com/documentation/uikit/uiapplication/applicationsupportsshaketoedit)

### 4.1b Android 平台事实（用户主力机：vivo + 豆包输入法）

Android 没有 iOS 那种系统级「摇动以撤销」提示，也没有 `DeviceMotionEvent.requestPermission()`；Chrome 的体感门禁是 secure context + Permissions-Policy（`accelerometer`/`gyroscope`），授权随 origin 授予、不弹系统框。[MDN](https://developer.mozilla.org/en-US/docs/Web/API/DeviceMotionEvent)

因此安卓侧的冲突面**不同**，不能照搬第 4.2 节：

1. 没有系统撤销提示可测 —— 但不等于没有误触：第三方输入法（豆包/vivo 内置）可能自带晃动或手势快捷操作，部分 ROM 有「抬起亮屏」「翻转静音」等系统手势，会与翻腕共用同一段运动信号。
2. 传感器组合与采样率因机型差异极大，必须能力探测，不能假定陀螺仪存在或频率稳定。[Android Motion Sensors](https://developer.android.com/develop/sensors-and-location/sensors/sensors_motion)
3. 真机门槛中的「系统摇动撤销 30 次」在安卓上替换为：抬起放下 / 转屏 / 走动 / 输入法晃动手势等干扰合计 50 次，错误发送 0 次。
4. 安卓的 `rotationRate` 与 iOS 单位一致但采样序列与抖动特征不同；阈值不得沿用 iOS 实测值，需独立标定（见 6.2）。

### 4.2 上线规则

1. 在「摇动以撤销」开启、有真实撤销历史的输入场景中测试（Android 侧替换为输入法撤销/重做历史与 ROM 手势可用的场景）。不要仅测空输入框，也不要只测刚刷新、没有编辑历史的页面。
2. 连续摇晃、任何不确定动作都丢弃，不缓冲待发；撤销动作出现一次错误发送即判该测试组合不通过。
3. 翻腕引出系统撤销提示也判共存测试不通过，不能通过提前发送或强行 blur 掩盖冲突。
4. 通过有限测试仅表示已测组合通过，不表示所有 iPhone、iOS 与浏览器不会冲突，也不表示所有 Android 机型与 ROM 不会冲突；交付中分平台列明设备、系统、浏览器/内核版本与摇动撤销或 ROM 手势开关状态。
5. 若当前设备共存不通过：保留设置整合，体感维持关闭；给出「此设备可能与系统撤销或手势冲突，请使用按钮发送」。用户主动选择关闭系统功能后可重新练习，但这是用户自报条件，不是程序验证结果。关闭系统撤销仍不能证明有意撤销动作不会被误识别为发送。
6. 无真机证据时只能交付练习/实验功能，不能默认开放真实体感发送。不要为了满足功能验收降低这条标准。
7. 双轨独立判定：iOS 与 Android 各自记录通过/失败，**任何一台不通过只关该平台**，不把另一台的通过结果外推。两台都未通过则整体维持关闭。

## 5. 权限与部署前置

先检测 `window.isSecureContext`、DeviceMotionEvent 和有效数据。需要 requestPermission 的实现由用户点击触发；无此方法不直接判不支持，尝试监听并超时判断。授权成功不等于传感器数据有效，零值可能是静止，null/非有限数不能当成零。[MDN 权限说明](https://developer.mozilla.org/en-US/docs/Web/API/DeviceMotionEvent/requestPermission_static)

- 当前局域网 HTTP 保持原操作可用，体感组就地提示「当前连接不支持体感，请使用安全连接」。不能让新功能阻断按钮发送。
- **安全上下文复用既有原生 HTTPS，不搭反代**：`SecureTransport` 已在 `:46487` 复用同一套 `route`/`serveFile` 提供完整手机页与 API，WS 在 `:46488`、画面帧在 `:46489`；证书由 `scripts/setup-secure-channel.sh` 一次性生成（自签 CA + 叶子证书，SAN 含 LAN IP 与 `<host>.local`），私钥不出电脑，已有身份不覆盖。手机信任 CA 后即获得 `isSecureContext`。旧 HTTP 直连路径继续可用。
- 执行前置：跑一次 `scripts/setup-secure-channel.sh`（该脚本遇已存在 TLS 目录会直接退出并保留现有证书；需重建时先备份 `~/Library/Application Support/VoiceDeck/tls`），在 iPhone 与 Android 分别安装并信任 CA（iOS 还需在「证书信任设置」中显式启用），再用 HTTPS 入口重新扫码配对。不要自动公开服务到公网或购买服务；Tailscale 跨网场景把 SAN 覆盖到私网地址即可，不因此改走反代。
- 鉴权边界已由现有代码覆盖，实现时只做核对、不做新增反代：写请求与 `/api/screen/*` 需 token 或回环豁免；WS 首帧（WSServer:98）与画面 watch（FrameServer:69）各自校验 token 或本地 peer。明确记录：`Server.swift` 的回环豁免对 secure 监听同样生效，仅因 loopback 物理上不可远程伪造而可接受——**绝不新增"信任客户端转发头"的判断**。
- 核对 WSS Upgrade、截图和输入路径在新入口下仍通；二维码应给出 `:46487` 的真实入口。HTTP→HTTPS 属于不同 origin，token 与 localStorage 偏好不会自动迁移；通过新二维码配对，不把"丢设置"误当代码回归。
- 不写绕过证书校验的教程作为正式方案，不以 API 存在或桌面模拟事件作为任一平台的真机成功证据。

## 6. 识别、门禁与状态模型

### 6.1 职责分离

传感器适配负责授权/订阅；识别器仅从样本产生候选事件；发送门禁决定是否执行；统一发送函数负责业务请求。不要让识别器访问 DOM 草稿、fetch、配对 token 或 Mac 协议。

识别器输入：时间戳、角速度、加速度/含重力加速度、设备实际方向。使用真实 dt，暂停或大采样间隔后重置；不把事件次数当时间，不二次积分加速度求位移。

短时间内角速度积分可用于相对旋转，重力方向可辅助回位判定；必须处理坐标与握姿。CSS `.rotated` 是画面旋转，不是传感器物理方向。第一版覆盖通过测试的竖屏/横屏握姿；未验证姿态明确暂停，而不是错误映射。

### 6.2 状态机与实验初值

```text
off / unavailable
       ↓ 用户开启且能力通过
stabilizing → ready → candidate → settle → triggered
                 ↑       └─反复摇晃/超时/门禁变化→丢弃
                 └──── cooldown + 回位平稳 ← triggered
```

实验起点（全部不是平台标准，集中配置并记录最终实测值）：静止约 400ms；单向轻翻约 20–35°、持续 150–450ms；结束停稳 250–400ms；冷却至少 1500ms，且回位静止后才重新 ready。总判定使用有符号旋转及运动序列，不根据单个加速度峰值。

候选窗口中往返旋转、多轴混乱、猛烈平移冲击、转屏、数据缺口均取消。识别必须有拒绝路径，未达标不自动提高灵敏度。停稳观察可降低连续摇晃误触，但不能数学保证区分系统撤销。iOS 与 Android 的阈值**分别标定、分别记录**，不得用一台的实测值外推到另一台（采样率与抖动特征不同）。

### 6.3 真实发送的门禁

候选开始与最终触发均检查：已开启且实测可用、页面可见、非设置/练习、非触摸操控中、非发送中、连接未判离线、存在可发送文字或图片、目标有效且未变。任何门禁变化取消本次候选，稍后状态恢复也不补发。

候选记录草稿 revision、目标 ID 和图片标识；候选到提交之间文字、图片或目标变化就取消。主页与全屏代理输入必须更新同一个草稿 revision，不能分别维护两份文本。

输入组合态时不发；最近文字仍在更新时不发（可从最后 input 起静默 700ms 作为实验值；注意现有 `LIVE_DEBOUNCE = 90ms` 是同步防抖，与它不是同一个量，不要混用或复用该常量）。系统听写可能停顿后继续输入，静默时长不等于听写结束，必须写入测试与用户说明，不提供「说完自动发送」承诺。

所有 pointerdown 到 up/cancel 期间暂停，覆盖触控板、画面拖动/捏合、PiP 拖缩、滑杆；返回后台、关闭、离线、横竖屏转换重置。恢复后重新静止校准，不能复用旧候选。

### 6.4 偏好与运行态

沿用旧触控板存储键；新增一个版本化翻腕偏好记录即可。只持久化用户意愿/练习信息，不持久化「已授权」「正在 ready」或发送锁。浏览器拒绝存储时内存可用并提示不保存。

状态区分：关闭、请求权限、练习中、已开启、已暂停/需重新授权、不可用。权限拒绝/无有效数据不能显示已开启；刷新后重新检查能力，不自动弹权限。能力有效前不启动真实识别。

## 7. 发送一致性：必须先验证再接入

复用并小范围整理 `send()`（compose.js:398），使按钮、两种键盘与翻腕共用业务锁及明确结果（跳过/已提交/未确认/失败）。业务锁 `submittingDraft` 已存在，**缺的是返回值**——现在 `send()` 是 void，只在内部 `message()`，翻腕入口拿不到本次结果；这才是本节的真实新增工作量。

关于队列：原方案写的「pushLive(B) 返回 A 的 Promise」**已不成立**。`compose-queue.js` 的 `ComposeQueue` 为每个入队项独立 resolve waiter，且 submit 不参与合并（`!value.submit && !tail.value.submit`），send 等到的就是自己的提交回执。不要再去"修"这个已修好的缺陷。

改测下面这些仍然成立或新出现的竞态：

- **前序失败连带拒绝在途 submit**：`ComposeQueue.drain()` 的失败分支会 `this.clear()` 掉全部在途项，包括排在后面的 submit B。用户敲字（A 普通同步）后立刻翻腕发送（B）时，A 失败会让 B 一起被 reject。先写测试复现，再决定这是可接受（草稿保留、提示可重试）还是应让已排队的 submit 独立存活。
- `clearCompose()` 与 `hideKeyboard()` 都会 `liveQueue.clear()`；提交期间的新输入不能因此被吞掉，也不能被重复提交。
- 普通草稿允许 latest-only 合并，但 submit 不能被后续普通同步覆盖、不能重复消费。

按可复现证据作最小修正：将普通同步与提交屏障语义区分，保证当前最后全文与 submit 的顺序，并让对应调用者拿到对应 Promise。不要并行新建第二条网络发送通路。

- 同时点击/按发送键/翻腕只接受一次；锁在任何 await 前取得，最终释放。
- 请求前快照目标、原文、图片与 revision。提交过程中继续输入的新文字不得被旧请求成功后的 clearCompose 删除；必要时只清除仍匹配已提交版本的草稿。
- 图片预上传仍在途时不能发送错误的 pendingImage；沿用图片就绪状态加入门禁。
- 请求失败或结果不确定不得自动重试，超时也不能视为服务器没执行。保留可恢复内容，沿用既有结果分级，不声称目标应用完成了业务消息发送。
- 全屏成功后按既有产品行为收键盘；若有新草稿则保留。反馈必须在全屏可见，不只更新被遮挡的主页消息行。
- 识别成功提示与发送结果分开。复用原消息/全屏 toast，iPhone 不支持网页震动时不影响反馈，不引入强制声音或震动依赖。

## 8. 建议代码边界与文档回环

建议最多增加 `Web/settings.js` 与 `Web/motion-send.js` 两个模块：前者负责面板生命周期和偏好视图，后者包含传感器适配与可纯函数测试的识别器。`app.js` 负责首页装配与旧齿轮解耦，`compose.js` 才是发送门禁与 `send()` 的所在地（不要到 app.js 里找发送逻辑）；只有纯识别器确有测试加载需求时才再拆独立文件。不要引入框架或泛用设置平台。

| 文件 | 预期改动 |
|---|---|
| `Web/index.html` | 移动唯一设置入口，移动原参数控件，新面板与脚本加载，更新头部契约 |
| `Web/app-extras.css` | 旧齿轮与 `.pad-tuning` 的主规则在这里：迁移并清理 `show-tuning` 耦合，补面板布局、窄屏、安全区与米白主题覆盖 |
| `Web/style.css` | 仅在基础形态层补面板与 header 两行 grid 所需令牌；不要把新规则塞进这里再靠 app-extras 覆盖 |
| `Web/app.js` | 删除 `exitPadMode()` 对 `show-tuning`/`padSettings` 的耦合，注入设置/体感依赖，header 装配 |
| `Web/pad.js` | 删除 177 行的齿轮 `show-tuning` 开关；触控板参数控件若迁出则同步移除取值逻辑 |
| `Web/compose.js` | **发送门禁实际所在地**：给 `send()` 加结构化返回值，翻腕/按钮/两种键盘共用；`sendFromKeyboard()` 改为依据结果收键盘 |
| `Web/settings.js`（拟新增） | 设置 UI，授权/练习由回调协作；不持有第二份业务草稿 |
| `Web/motion-send.js`（拟新增） | 授权、采样、识别、复位、销毁；只通过回调交出候选。识别器保持纯函数以便 `tests/` 用 `require()` 直接加载断言 |
| `Web/screen.js` | 最少量设置互斥/交互忙碌/全屏反馈协作；含与 `#screen-menu` 的 dialog 互斥，保留既有手势和键盘逻辑 |
| `Sources/Server.swift` | 新脚本加入 460 行的显式静态白名单；无反代，不需要改动鉴权结构 |
| `scripts/install-app.sh` | 已整体复制 Web，核验新脚本进入 bundle；无必要不改脚本 |

业务文件已有超过 800 行的情况：新功能独立成模块，触及发送部分只做必要抽取，不发起全仓重构。执行前读 L1/L2/L3；新增文件补 L3 与 L2 成员，接口变更同步契约；新增资源数量、路由、部署说明同步 L1/README。实现后再将本方案决策写入 DESIGN.md，标明哪些限制仍在。

现有 HTML / screen.js / style.css 部分头部与现实不一致，执行修改到该文件时校正相关职责描述，不照抄过期头部。所有手机资源 `?v=` 同步升级，新增脚本同版；确认打包应用和手机缓存真的加载新文件。

## 9. 实施顺序与交付门槛

1. **设置整合**：迁移一个入口及三个旧控件，完成焦点/遮罩/键盘回归；翻腕组默认关闭，未具备条件时解释原因。该步不依赖 HTTPS，可以单独交付。
2. **传感器练习**：跑 `scripts/setup-secure-channel.sh` 后让 iPhone 与 Android 各自信任 CA，用 `:46487` 入口做点击授权、有效样本检测和只读练习；**先各自测本平台的冲突面**（iOS 系统摇动撤销 / Android ROM 手势与输入法晃动）。任一平台无真机则止于实验状态并报告待测。
3. **发送屏障验证**：用可控延迟和失败响应复现第 7 节竞态（含新增的「前序失败连带拒绝在途 submit」），修正后跑按钮/键盘/图片回归。此阶段不依赖实际甩动。
4. **受控接入**：完成识别器、门禁、生命周期、全屏反馈与 `send()` 返回值；鉴权结构沿用现有实现只做核对。仅对通过本平台验证的场景开放用户主动开启，按平台分别开关。
5. **收尾交接**：提交改动摘要、**分平台**设备测试记录、失败/不可用路径、构建结果、资源版本（当前基线 style `3.0.10` / app.js·compose.js `3.0.22` / screen `3.1.0`，新增脚本同版并逐个升位）及待测项；不要把"代码完成"写成"iPhone 验证通过"或"Android 验证通过"。

## 10. 验收清单

### 页面与设置

- 320×568、390×844、横屏及键盘开启时无横向溢出；全屏/PiP 退出和返回不丢草稿、不误发远端操作。
- 手机主页面设置入口总数为 1；触控板内部、发送行、全屏 HUD 没有新增齿轮或体感开关。
- 原有三个触控板参数值、范围、持久化与效果保留；既有 HTTP 用户不因体感不可用失去其他功能。
- 设置内部可滚动，关闭和标题可见；触控区至少 44px，VoiceOver、Esc、焦点恢复和两套主题通过检查。
- 打开设置不发远端点击，关闭不穿透，练习不产生 `/api/send` 或 submit 请求；原来的草稿实时同步不是练习发送。

### 自动化与确定性测试

- 对识别器输入静止、翻腕、连续摇晃、放下冲击、采样缺口、姿态切换轨迹，断言触发/拒绝与单次触发；使用真实录制片段补充，合成轨迹不能证明系统兼容。
- 可控 Promise 验证 A 在途/B 提交/B 失败、普通同步覆盖、双入口并发、提交后新输入、目标变化和超时不自动重试；**新增**：A 普通同步失败时在途 submit B 是否被 `ComposeQueue.clear()` 连带拒绝（先复现，再定行为）。
- `send()` 返回值枚举断言：跳过（无内容/无目标/组合中）、已提交、已发出未确认、失败，四个分支各自可构造。
- 验证关闭/后台/练习结束后不残留传感器监听或延迟触发；禁用期间无高频采样。
- HTTPS 入口下有效 token 正常，缺失/错误 token 的远端写入、画面读取和 WS 被拒；旧合法直连行为不回归。
- 新脚本经实际服务返回 200 与正确 MIME，打包后也可加载。按实际工具链完成构建和必要回归，不额外创建纯镜像实现测试。

### 真机发布门槛（iOS / Android 双轨）

两台都要逐个记录：设备型号、系统版本、浏览器及内核版本、相关系统开关（iOS 摇动以撤销 / Android ROM 手势与输入法晃动手势）、键盘/听写状态、横竖屏、主页/全屏。**iPhone 至少 Safari 必测，Android 至少 Chrome 必测**（用户主力为 vivo + 豆包输入法）；主屏幕安装模式与其他浏览器只有测过才列为支持。

通用门槛（两平台同样适用）：有意翻腕 30 次，成功至少 27 次、重复提交 0 次；干扰动作合计 50 次，错误发送 0 次。

- iOS 干扰与零容忍项：主动摇动撤销 30 次；翻腕引发系统撤销提示 0 次。
- Android 干扰项：抬起放下、转屏、走动、输入法晃动手势、ROM 翻转/抬起手势。

数字是项目首轮门槛，不是统计上的零风险保证，任何冲突都要收紧范围或关闭该组合。**两台独立判定，一台不通过只关该平台。**

先在不会产生外部消息的测试目标输入框完成全部动作；再由用户验证日常目标（含飞书等真实应用）。特别覆盖末字尚在同步、刚停听写、中文选词、附图、断线重连、应用切换及 Mac 前台变化。

## 11. 给执行模型的启动指令

按本文件执行「统一手机设置 + 翻腕发送」。基线为 `f0ff85e`（资源版本 style `3.0.10` / app.js·compose.js `3.0.22` / screen `3.1.0` / unlock `1`），先阅读根目录与 Web/Sources 的 CLAUDE.md、DESIGN.md 和当前代码重新核对；保留其他修改。

设置只留顶部一个入口，迁移触控板旧设置（旧齿轮样式在 `app-extras.css`，开关键在 `pad.js:177`，`exitPadMode()` 耦合在 `app.js:197`），禁止在发送行与全屏 HUD 增加齿轮。发送门禁接在 `compose.js`，不是 `app.js`。

按第 9 节顺序落实，优先验证：iOS 系统撤销共存与 Android ROM/输入法手势冲突（双轨）、`:46487` 原生 HTTPS 下的传感器练习、实时同频提交屏障（注意 `pushLive` 的旧缺陷已由 `ComposeQueue` 修复，改测第 7 节列出的新竞态）。安全上下文用现有原生 HTTPS，**不搭反代**。

所有物理阈值以真机数据定稿，两台分别标定；无法取得真机时交付可验证的设置与练习模式，明确真实发送待测。不要自动关闭系统设置，不要公开服务到公网。完成必要测试与 GEB 文档回环（`Web/CLAUDE.md` 成员清单与修订条目、`CLAUDE.md`、`README.md`、`DESIGN.md`），再分平台报告已完成、已实测和仍受限制的部分。
