# PocketDesk 全屏手机操控优化方案

> 后续规划入口：`FULLSCREEN_WORKSPACE_PLAN.md`。本文件部分建议已进入 v2.9.23，现状表与部分交互决策不再代表最新实现；后续全屏改造以新方案的源码复核、交互定义和阶段验收为准。本文件保留历史依据。

> 2026-09-09。代码审查与设计建议，尚未实施、未进行手机与 UU 对照实测。
> 用户目标：全屏看电脑时，手机上点哪里就操作哪里；单击、双击可靠，移动和结果反馈跟手。
> 与 CURSOR_VIEW_PROPOSAL.md 配套；本方案聚焦全屏交互，后者的观测值与命令期望值分离原则继续有效。

## 1. 核心建议

优先改成「绝对定位 + 即时触点反馈 + 可靠单/双击 + 全屏实时光标」，随后提升画面回传帧率。不要用调高触控板灵敏度来解决全屏点击问题。

“指哪打哪”是绝对定位：手机画面上某个位置，映射到电脑上同一个位置。它不需要鼠标加速曲线。相对触控板才需要灵敏度设置，两种操作不能共用一个倍率。

UU 作为用户主观体验标杆。网易官方 App Store 页面强调低延迟与高帧率，但没有公开足以照搬的手势阈值、注入协议和延迟测量条件；本文不把宣传数字当作本项目性能承诺，也不声称已经复刻其手势。[1]

## 2. 现状诊断：哪些已经从代码确认

本项目在其他任务中持续修改。以下按 2026-09-09 再次读取的源码记录，执行者必须重新核对，不能照旧行号盲改。

| 代码位置 | 已确认行为 | 对体验的影响 |
|---|---|---|
| Web/screen.js capture() | 每次截图请求结束后 scheduleNext(1000) | 实际间隔是请求耗时再加一秒，并非稳定 1fps；点击已执行也可能迟迟看不到结果 |
| syncCursorSubscription() | 最新版本 wanted=false，整个叠加订阅已停用 | 当前依赖截图中的鼠标，位置反馈同样迟缓 |
| paintCursor() | 仍有 !isFullscreen() 条件 | 仅恢复旧订阅条件也无法让全屏得到实时光标 |
| sendTap() | img 的 click 转为带绝对坐标的 tap，click=true | 具备点到指定位置的基础，但没有完整全屏双击识别 |
| 全屏 Pointer Events | 放大后单指平移本地画面，双指捏合 | 手指滑动并不连续控制远端鼠标；调 sens 不会改变这条路径 |
| DRAG_CLICK_GUARD | 拖动结束后 400ms 内忽略 click | 拖完立刻点一次可能被吞掉，需要按手势归属抑制，不能全局封禁下一次点击 |
| PointerExecutor 的 tap | 左键 down/up 默认 clickState=1，当前已有 40ms 间隔 | 两个 tap 没有显式构成 clickState 1→2；40ms 不是唯一瓶颈，不应无证据删掉 |
| app.js lastTap 与服务端 click | 第一下 count=1；第二下 count=2；服务端把 count 当完整点击次数循环执行 | 两次轻点可形成三组 down/up，这是协议语义不一致；触控板修复时不能照搬到全屏 |
| 当前旋转/输入联动 | 有全屏旋转坐标辅助与 pocketdeskAfterTap() | 新状态机必须保留旋转、键盘联动，并避免双击中途弹键盘改变布局 |

这些是静态代码事实与推导，不代表全部问题已在真机复现。网络抖动、浏览器布局成本、权限和具体应用响应时间仍需测量。offset 坐标当前是否错误也不能仅靠代码风格断言，需要几何测试。

最新代码注释说叠加层曾因主线程布局开销被停用。恢复时应缓存几何并测量，不能简单把 wanted 改为 true 后宣称性能问题解决；读取 offsetWidth 不一定每次触发布局，是否发生强制同步布局须用性能记录确认。

## 3. 推荐手势：默认直接控制

| 手机操作 | 默认行为 |
|---|---|
| 单指轻点 | 在手指对应的远端位置单击一次，松手立即发出，不等待双击超时 |
| 同一位置快速轻点两次 | 标准双击，总共两组 down/up；第一下仍立即执行 |
| 单指滑动 | 绝对位置跟随移动鼠标，不按左键；滑动结束不补单击 |
| 单指按住达到阈值后拖动 | 在起点按住左键，拖动远端对象，松手释放；进入拖动给局部反馈 |
| 双指捏合/同向移动 | 只缩放/平移手机上的画面，不给 Mac 发点击或 Cmd+滚轮 |
| 右键 | 小型工具栏明确按钮，作用于最近有效远端光标位置 |
| 页面滚动 | 独立滚动条或明确滚动工具，首版不让双指同时承担本地平移和远端滚动 |

这会改变现有「放大后单指平移画面」规则：全屏单指统一控制电脑，双指统一操作视野。须更新提示及 Web/CLAUDE.md，不能让两个监听器同时生效。PiP 拖动窗口维持原行为。

直接控制适合按钮和大目标。小字号编辑、远距离移动可后续提供「触控板」可选模式，复用相对控制；不要求用户为了基本单击先切多个模式。放大查看应先作为小目标精确点击的主要手段。

单指按下先记录候选和触点，不立即给 Mac 发左键 down，避免第二根手指加入捏合时已经误点。超过移动阈值进入移动，超过长按阈值且仍稳定才进入拖动；两者互斥。初始实验可用 6–10 CSS px 移动阈值、约 400ms 长按阈值，须真机调参，非最佳实践标准值。

不默认加入磁吸按钮、落点预测或自动寻找最近控件：这些会让真实落点偏离用户所指位置。

## 4. 坐标只有一套转换

提取共享几何模块供点击、连续移动、拖动、光标绘制、触点反馈使用。Pointer Events 捕获后 event.target 可能改变，不继续依赖 img 专属 offsetX/offsetY 作为全局协议。

输入侧流程：

1. 取 clientX/clientY，进入稳定的画面容器坐标系；明确其 border/padding、visual viewport 和布局原点。
2. 对父级旋转、平移、缩放求逆变换，回到未变换内容坐标。当前存在旋转辅助，应复用并统一，不能重复转两次。
3. 减去 object-fit:contain 产生的留白，除以实际图像显示宽高，得到 rx/ry。
4. 留白或 HUD 点按不发远端命令；非法数值/NaN/Infinity 拒绝，不默认当作零点。
5. 服务端用选定显示器的 CGDisplayBounds 映射到全局逻辑坐标，保留负坐标与边缘钳制。Retina 截图像素和 Mac 逻辑点不直接相乘。

无旋转、容器局部点为 p，平移 t、缩放 s、适应态图像矩形 b 时：

    local = (p - t) / s
    rx = (local.x - b.x) / b.width
    ry = (local.y - b.y) / b.height

有旋转时使用完整逆变换，不能仅取旋转后 getBoundingClientRect 的轴对齐包围盒做线性除法。输出侧用同一变换正向绘制光标，正反变换应可往返验证。

几何仅在 resize、旋转、图片尺寸变化、键盘导致视口变化、缩放/平移时更新，连续光标帧只读取缓存并写 transform。不要逐帧多次读布局再写样式。

每个手势绑定 displayId 和视图 generation；切屏、横竖屏变化、全屏退出、键盘弹出引起布局变化时取消当前手势，不让旧坐标落到新画面。收到首帧前不能操作；切屏后必须等属于新显示器的已解码帧，不能用旧图配新 displayId。

## 5. 单击、双击和拖动的协议修正

### 单次物理点击与完整双击动作分开

建议新增版本化的绝对指针命令（名字可沿仓库规范，语义必须明确）：

    {t:"pointer", sessionId, gestureId, seq, display, rx, ry,
     action:"move"|"click"|"down"|"drag"|"up", button:"left", clickState:1}

click 表示恰好一组 down/up；clickState=2 表示这是双击的第二下，绝不表示再点击两次。兼容旧 click.count 时保留它的旧含义，新的前端不可混用。独立「双击」按钮若以后增加，才由一次 doubleClick 动作生成两组事件。

自然双击：第一下立即发 clickState=1；第二下在时间、位置、显示器、会话条件匹配时发 clickState=2。Mac 的 NSEvent.doubleClickInterval 是系统双击时间来源，可以通过能力信息下发。[2] 服务端仍核验两次实际到达/执行间隔，网络间隔过长应作为普通单击并重置序列，不重放第一下“补双击”。

位置容差在手机 CSS 坐标判断，同时限制对应远端偏移，避免缩小画面时相邻两个按钮被合并。通过轻点位置、时间和变换版本判断；第二次轻点不能跨屏、跨旋转或跨控件布局代际。对小幅手抖可在限定范围内沿用第一下落点，但不得大范围吸附。

新协议上线后，修复已有触控板 count=1 再 count=2 造成三次点击的问题，并测试旧客户端兼容。双击测试核对实际事件数和应用行为，不只看日志显示了 double。

### 手势状态机

至少明确 idle、pendingTap、moving、remoteDragging、viewTransform、cancelled。第二根手指加入后本次接触永不回到 tap；剩一根手指也不能在抬起时补点。拖动期间出现第二指，先释放远端按键，再切换视野操作。

全屏统一在 Pointer Events 层识别，使用 setPointerCapture 及 pointercancel/lostpointercapture 收尾。[3] 同时移除或隔离旧 img.click 注入，避免 pointerup 与兼容 click 各发一次。原先 400ms 拖后屏蔽改为仅消费本次手势产生的兼容事件，下一次新轻点立即可用。

up/cancel、退出、断线、切屏必须释放左键并清计时器。服务端按连接/会话维护所有权，不能任意第二个手机连接就重置第一个的拖动。异步安排的点击 up 或第二下需检查会话代际；清理后不能又执行旧定时器。

Mac 点击仍需正确的 down/up 和 mouseEventClickState；现有 40ms 保留间隔可作为初始兼容值，是否缩短由应用验收裁决。避免在网络队列 sleep，执行队列内明确事务顺序，不能让异步 up 越过下一条 down。

键盘联动：只有已完成的轻点才调用现有焦点检查。双击候选窗口内延后弹出手机键盘（不延后第一下远端点击），以免第一下改变手机布局破坏第二下；移动、捏合、拖动不弹键盘。若延后影响常用输入，提供明确键盘按钮，按真实任务测试决定。

## 6. 跟手反馈分三层

1. 本地触点环：在下一渲染帧显示手指接触位置，样式与鼠标箭头不同，只代表收到触摸，不表示 Mac 已执行。
2. 权威光标：恢复全屏 CursorMonitor 订阅与叠加，用 Mac 实际位置绘制；不要把客户端预测坐标写进 CursorMonitor，也不要用它改 PointerExecutor 的 expected。
3. 桌面变化：由新画面证明按钮选中、菜单打开、窗口拖动等结果；光标移动回执不代表目标应用已完成动作。

订阅规则改为查看器可见、全屏控制开启、画面更新正常、WS 已鉴权。PiP 默认仍可沿用烘焙鼠标，避免为本任务恢复其已停用的高频开销。

光标渲染检查时效、当前显示器、页面可见性和最近成功解码画面时间，不能只看 updating=true。当前截图请求可能长时间挂起而 updating 仍为真；设可测的过期阈值并暂停操作，提示「画面已暂停，刷新后继续」。不要把唯一错误写进手机看不到的 console。

只有实际能绘制权威光标时请求 cursor=0；断线或不适用时退回 cursor=1。切换期间必须处理旧帧烘焙鼠标，避免双指针；可在新的无鼠标帧到达后启用叠加。仅改变订阅布尔值不算完整修复。

保留简洁界面：正常时不加常驻调试条；断线、权限不足、显示器失效才显示简短可操作提示。

## 7. 网络与画面优化的先后

### P0：先把点按、双击和真实光标做好

修手势/坐标、点击次数、全屏订阅、布局缓存。使用现有 WS 控制，不等截图返回再发送点击，不每次点击新建 HTTP 连接。不改全局相对灵敏度，不动语音同步逻辑。

### P1：减少看见结果的等待

点击/滚动后请求尽快获得新帧，但只保留一个截图在途；若旧帧已在捕获，标记需要后续新帧，不能把旧帧当点击后的结果。不要排几十个截图请求。

先测试低分辨率、较低 JPEG 质量、交互时短时提高取帧频率、空闲降频。2–5fps 只能作为单帧路径的实验目标，实际受 captureImage/编码/网络限制，不能将计时器改成 16ms 就宣称 60fps。

配套记录截图获取、JPEG 编码、字节数、传输、解码与显示耗时。服务端可缓存短期显示器元数据并在拔插时失效，避免每帧重复枚举；必须以测量确定价值。

### P2：要持续接近远控软件手感，升级画面通路

当前约一秒级截图无法提供流畅的窗口拖动与滚动反馈，30Hz 光标只能改善指针，不能让桌面内容也流畅。若 P1 仍达不到验收，需要 ScreenCaptureKit 持续捕获、硬件编码和低延迟媒体传输；评估 WebRTC 视频而非高频大 JPEG 堆在控制 WS 中。[4]

以 720p/1080p、30fps 作为试验起点，先验证延迟、文字可读性、CPU 与手机温度，再考虑 60fps。私网直连优先，但信令、ICE、编解码兼容和弱网仍需设计；不把“用 WebRTC”当自动解决延迟。

视频媒体流与控制消息分离；队列过载时丢旧视频/绝对 hover 位置，不能丢按键边界。绝对移动可在同一手势段内保留最新位置；相对位移需累加，拖动画线可能需要轨迹，不能无差别 latest-only。down/up/click 顺序必须保留，拖动结束先处理最终位置再 up。

普通 WebSocket 没有自动背压，监控 bufferedAmount，限制待发长度与年龄。[5] 延迟积压后暂停控制并重建会话，重连不能补发旧点击。新 ack/error 走可靠有界队列，不能和 CursorMonitor 共用 WSServer 现有单槽可覆盖 pending。

## 8. 验收与 UU 对照方法

不用用户工作内容做点击实验，创建无破坏性测试窗口。记录手机型号/浏览器、Mac/显示器、横竖屏、缩放比例、Wi-Fi 条件及代码版本。

| 测试 | 通过条件 |
|---|---|
| 5×5 网格定位 | 1x/2x/6x、平移、横屏和当前旋转模式下落点一致；映射自动测试误差 ≤1 Mac 逻辑点，边缘钳制除外 |
| 真人点击不同大小目标 | 报告命中率、完成时间与修正次数，不能只测超大按钮 |
| 连点两次/三次、相邻两点 | 双击恰好两组 down/up，不出现三次点击；不同位置保持独立单击 |
| 拖完立即新点 | 新轻点不被 400ms 全局窗口吞掉 |
| 捏合、第二指加入、旋转、弹键盘 | 不误点、不卡按键、不沿用旧几何 |
| 拖窗口、选中文字 | 远端按下/移动/释放完整，无中途跳点；与本地画面平移互斥 |
| 断线、权限撤销、换屏、拔屏 | 停止操作并释放按键，不补旧点击，不静默点主屏 |
| 实体鼠标与手机交替 | 真实位置可重新对齐，不能因 expected 缓存跳回旧位置 |
| 连续 10 分钟操控 | 队列、内存不持续增长，后台停订阅，记录功耗/发热 |
| 回归 | PiP 拖动缩放、主页触控板、屏幕切换、键盘联动、实时文字输入保持可用 |

延迟分别测量：本地触点显示；手指到 Mac 指针/点击事件；手指到手机看到真实结果。手机与 Mac 时间不可直接相减，使用同设备 RTT 和同时拍到手指、Mac、手机的高帧率录像；记录 P50/P95，不只报最快样本。

健康局域网初始目标（不是实测结果）：本地触点下一帧显示；权威光标回显 P95 <150ms；持续视频阶段真实桌面变化回显 P95 <200ms。单帧截图阶段单独报告，不用本地触点速度冒充远端响应速度。

UU 对照用同一手机、Mac、网络和目标，测试点选、双击打开测试文件、拖动测试窗口、滚动阅读四项；注明其当前操作模式。比较任务完成时间、误触/重复次数与延迟录像，不要求复制未验证的 UU 手势细节。

## 9. 给执行模型的交接指令

请先读当前 CLAUDE.md、Web/CLAUDE.md、Sources/CLAUDE.md、CURSOR_VIEW_PROPOSAL.md 与本方案，核对现有未提交改动。优先实施 P0/P1：全屏绝对定位、统一 Pointer Events 手势、正确单/双击、可取消的远端拖动、双指本地视野变换、全屏权威光标及几何缓存、交互后及时刷新。修复触控板 count 语义导致的三次点击，但保留旧协议兼容。不粗暴调高 sens，不默认恢复 PiP 的高频叠加，不重写语音输入、不覆盖其他任务改动。

将几何换算与手势状态机从 screen.js 按职责抽出，避免继续堆大文件；每个新业务文件补 L3，更新 L2 成员和根 L1、必要的 DESIGN/README 行为说明、index.html 资源版本。测试几何、事件序列和取消边界，再做手机真机验收。P2 先提交测量结果与媒体通路方案，再作为后续独立实现；不能把 P0/P1 宣称已达到持续视频远控水平。没有真机或 UU 对照条件时明确标注未测。

## 10. 参考来源

1. [网易 UU 远程 App Store 官方上架页](https://apps.apple.com/cn/app/id1642306791)：只用于核实产品定位与公开能力，宣传性能不是本项目基准。
2. [Apple NSEvent.doubleClickInterval](https://developer.apple.com/documentation/appkit/nsevent/doubleclickinterval)：系统双击间隔。
3. [MDN Pointer Events](https://developer.mozilla.org/en-US/docs/Web/API/Pointer_events)、[setPointerCapture](https://developer.mozilla.org/en-US/docs/Web/API/Element/setPointerCapture)：统一触摸事件、捕获及取消边界。
4. [Apple ScreenCaptureKit](https://developer.apple.com/documentation/screencapturekit/capturing-screen-content-in-macos)、[MDN WebRTC](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API)：持续捕获与实时媒体路线；具体传输架构需单独验证。
5. [MDN WebSocket](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket)：背压限制及缓冲风险。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
