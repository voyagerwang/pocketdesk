# 全屏工作台首版交接

2026-09-09；Web 基础资源版本 3.0.0；画面相关资源更新为 3.0.2（自动横屏与坐标适配）。键盘输入栏资源更新为 3.0.3。规划依据为 FULLSCREEN_WORKSPACE_PLAN.md，本文描述实际代码与验证边界。

## 每阶段已落地的效果

| 阶段 | 用户现在能看到的变化 | 主要执行内容 |
| --- | --- | --- |
| 操作可靠性 | 黑边不误点、缩放后点击仍对应画面；取消或第二根手指加入会先松开拖动；发送失败保留草稿 | 共享坐标模型、统一手势取消、控制租约、独立提交队列、输入上下文校验 |
| 全屏工作台 | 工具栏和输入区保持正向；默认滑动滚动，可切指针；键盘打开后显示单行草稿与换行/发送/收起图标，异常才显示提示 | 独立画面/工具栏/编辑区网格、视口适配、模式/长按菜单、PiP 与逐层返回 |
| 持续画面 | 全屏优先连续更新，卡流自动切回截图；画面陈旧时停止控制 | ScreenCaptureKit 15fps 目标采样、最新帧编码、独立 WS、浏览器呈现确认与超时 |

这三项的代码已经接通，但“真机体验达标”仍需下面的人工验收。当前不能把目标帧率、端到端延迟或 UU 同等级流畅度作为已验证结果。

## 实现边界

- 控制：认证返回 session/controller/absolutePointerV1/doubleClickMs；客户端每 500ms 心跳，服务端 2 秒租约。移动按帧合并，点击/松键等离散命令有序；仅当前控制者可注入。旧客户端的相对/点击格式仍可接收。
- 画面：HTTP 端口 +2 的独立 WS，watch 首帧认证并指定 display/width。二进制为 4 字节大端 JSON 长度 + JSON 头 + JPEG；头含 frameId/epoch/display/width/height/cursorIncluded/ageMs。浏览器解码呈现后发送 presented/frameId。
- 背压：采集端只留最新待编码帧；每个观看者一帧在途、一帧最新候选；确认超时断开。相同显示器和宽度共享采集器，最后一个观看者离开时停止。画面不落盘。
- 降级：持续帧失败后单帧截图，交互后优先补拍；静止时约 1 秒间隔，仍限定一个请求在途。连续画面不烘焙鼠标，截图包含鼠标，两者不叠画。
- 输入：输入位置绑定元素（可识别 AX 时）或应用（无法识别时），有效期 10 分钟。每次绑定输入在实际执行队列检查控制权及焦点。首页与全屏共享同一焦点基线，避免只切换界面就重复输入。
- 字段同步没有改造成完整文档协同。用户在电脑手动修改文本、移动光标或在 AX 不透明的同应用编辑器间切换，仍可能使差分基线失效；本版不具备这些行为的可靠读回检测。
- 全屏上下文输入不自动寻找别的输入框；原首页显式唤醒/聚焦流程继续保留。历史图片与快捷键流程未重做。
- 不含 WebRTC、音频流、远端显示器旋转、独立手机 App 或公网 TLS 部署。

## 已执行的验证

- `swiftc Sources/*.swift ...`：完整链接构建成功；保留 Server.swift 原有 Any? 与 Swift 并发捕获告警，未作为 Swift 6 严格并发通过来宣称。
- `node tests/screen-core.test.js`：坐标往返、黑边排除、拖动取消、双指接管松键、草稿合并、提交独立完成和错误传播通过。
- `python3 tests/workspace_browser.py`（需 Playwright/Pillow）：390×844 竖屏、844×390 横屏、可见中文/emoji 输入、中心点击、切模式/切屏/返回、连续帧呈现 ACK、停流降级、提交失败保留草稿、失去控制权禁用输入通过。
- 浏览器测试全部使用本地静态文件和模拟 HTTP/WS；没有向真实桌面注入按键，也没有向聊天或 Agent 提交测试消息。
- 截图保存在 `/tmp/pocketdesk-portrait.png`、`/tmp/pocketdesk-compose.png`、`/tmp/pocketdesk-landscape.png`，用于本次布局核对。

## 安装后的实际验证

- 实际 HTTP 服务、控制 WS 和画面 WS 已启动；辅助功能与录屏权限正常。
- `node tests/runtime-smoke.cjs` 通过：当前 1512×982 显示器、1280 宽度 JPEG；约 12 秒本机回环测试收到 173 帧，观察到约 14.33fps，平均帧约 104KB，帧头报告的最大捕获年龄 150.7ms，期间状态请求最慢 27.3ms。捕获年龄不是手机端到端延迟，这也不是局域网或 Tailscale 性能结论。
- 观看连接退出未关闭另一条控制连接；不再确认呈现后只继续收到至多一帧，随后因超时关闭，背压检查通过。
- `python3 tests/runtime_browser.py` 通过：真实 JPEG 解码并确认 13 帧；320×568、390×844、844×390、844×220 四种视口按钮全部在可视范围；退出查看器停止新帧。
- 临时原生文本窗口的真实输入测试未能确认前台身份，守卫在落键前拒绝继续，未向其他应用注入测试内容。真实中文、换行及提交仍列为人工验收项；之前的模拟输入回归通过不能代替这一项。

## 启用与真机验收

已按用户要求安装并重启 `/Users/cm/Applications/PocketDesk.app`，签名校验通过。旧版本保留在 `/Users/cm/Applications/.PocketDesk-backup-20260909-125507.app`。服务端已返回 3.0.0 页面，手机刷新原页面即可使用新版。后续可通过项目既有 `./scripts/install-app.sh` 更新。仅刷新网页不会升级旧 Mac helper；只启动第二个副本也可能因端口占用继续连接到旧实例。

1. iPhone Safari / Android Chrome 各验证一次：竖横切换、浏览器地址栏变化、软键盘升降、中文候选与听写、连续发送 10 次、清空后再输入、失败草稿恢复。
2. Retina / 缩放分辨率 / 竖向副屏上点击四角和细小目标，双击、拖动窗口、滚动长文；拖动中切后台、切屏和断网，确认无粘键。
3. 两台手机依次观看/接管：观看者退出不影响控制者，旧控制者的后续输入被拒绝；Mac 输入焦点变化时草稿仍在手机。
4. 在局域网与 Tailscale 各持续阅读/滚动 10 分钟，记录实际帧率、延迟、CPU/内存与功耗；断网恢复确认无旧指令回放。根据实测决定是否调低采样率/画质或继续研究视频编码。

上述手机、多屏和跨网络完整验收仍待用户执行；桌面本机已完成的部分以“安装后的实际验证”为准。完成后再决定是否作为稳定版发布。

## 验收调整：自动横屏

进入全屏请求 landscape 方向锁；不支持时，在竖向画面视口内旋转画面/光标/触点坐标层。输入的绝对位置、相对位移、拖动与捏合统一逆变换。退出恢复 PiP 并释放方向锁。纯几何测试覆盖旋转后多级缩放往返；模拟浏览器新增竖屏旋转、非中心点击与物理横屏撤销回退检查。浏览器方向锁在实际手机上的支持情况仍需真机确认。

## 键盘简化验收修正（2026-09-09）

移除全屏输入目标标题、展开按钮和正常同步提示，保留 44px 图标点击区及单行可编辑草稿；失败保护与 IME 重建不变。Node 核心断言和隔离浏览器回归通过（6 次模拟输入请求、3 张截图、3 帧呈现），验证图标无文字、输入栏高度、清空重建、提交与失败保留草稿。手机软键盘及听写真机表现待验收。

## 切屏与滚动修正（Web 3.0.4）

新增工具栏显示器快捷轮换和独立滚动开关。切屏停止旧会话、隐藏旧图、清空几何和光标后获取新图；滚动模式按触点定位，竖直手势不受自动横屏旋转影响，滚轮补齐位置与结束相位。Node 和模拟浏览器回归通过：分别验证显示器 1/2 的帧请求及不同图片尺寸、双向切换、滚动与结束、320×568 / 844×220 / 844×390 下六入口未溢出。真实双屏与手机滚动手感仍需验收。

## 小窗切屏（Web 3.0.5）

小窗底部增加全部已发现显示器的编号按钮，当前屏幕高亮，与全屏共用切换状态。浏览器回归验证小窗 1→2→1 的图片尺寸与选中状态，进入全屏后隐藏小窗入口。

## 小窗精细化（Web 样式 3.0.6）

小窗操作按钮缩至 28px（图标 15px），屏幕标签缩至 26px 高，当前屏幕采用浅底描边。浏览器验证实际尺寸和双屏切换通过。键盘已根据用户截图记录双模式方向，待确认使用设备/输入法以确定原生悬浮键盘支持；本次未更改输入行为。

## 安卓原生输入法 / 电脑键盘（Web 3.0.7）

默认原生输入代理，底栏切换输入法/电脑键盘。正常实时替换隐藏重复编辑框，暂存与异常显示草稿；回车恢复换行，发送按需出现。电脑键盘覆盖画面并支持字母、数字、常用按键和一次性修饰组合，复用既有快捷键 HTTP 接口，前端校验可控状态、串行发送并在退出后取消排队。浏览器回归覆盖模式互切、Ctrl+A 后 Return 不带残留修饰、暂存草稿、提交/失败、IMЕ 重建；均为模拟接口，安卓豆包/vivo 实机听写与悬浮形态需验收。

## 持续未就绪修复（Web 3.0.8）

现场只读检查：辅助功能已授权，两个显示器取帧一度返回 422“显示器已断开”，随后列表为空，再次查询恢复为两个显示器且单帧 200；恢复时 locked=false，不能将该次临时故障断言为锁屏。补齐过期画面的锁屏状态查询和限频重新枚举；错误不再被泛化提示覆盖。观看者定时重新认证只领取空闲控制权，不抢占现有控制者，同 session 保留递增序号。修复停止采集被清理手势的 interact 重新激活的问题。模拟回归覆盖锁屏/解锁恢复、重认证序号和既有键盘/切屏流程。

## 自绘键盘撤除（Web 3.0.9）

用户真机验收确认自绘电脑键盘布局不可用，已删除按键面板、电脑键盘入口及专属按键队列/组合键逻辑；只保留豆包、vivo 等原生输入法、按需发送和关闭。此前双模式设计已作废，不再作为当前实现依据。

## iPhone 触控板与页面防误触（3.0.10）

收起时滑动触控板区域仍滚动页面，轻点只展开；展开后手势由起点确定归属，触控板内不带动页面。右缘滚动有 8px 防抖，轻点/长按不再误发点击/拖动；外部触碰立即停止惯性。保留页面外滚动，不锁整页。

## 滚动流畅度（3.0.11）

全屏补齐按时间衰减的可取消惯性、触点合并采样、直接触摸显示比例换算；30fps 采集目标替代首版 15fps。Node 验证 60/120Hz 惯性距离差小于 1 逻辑像素、取消立即结束、停顿后松手不甩动；模拟浏览器完整回归通过。Swift 编译通过，已有 Server Sendable/可选值警告未改动。真机滚动手感仍需验收。

安装后本机真实服务 12 秒只读测试：348 帧，约 28.88fps，最大帧年龄 42ms，平均 JPEG 157KB，状态接口最高 12.2ms；未确认画面时超时关闭通过。与旧版不同场景，不能作为严格 A/B；这也不是手机端端到端延迟。原二进制备份于 /tmp/PocketDesk-before-smooth-VoiceDeck。

## 点击与滚动统一、精确瞄准（Web 3.0.12）

删除独立滚动入口，触屏/指针切换移到更多，默认轻点点击、滑动滚动。长按改成 3 倍局部放大瞄准，位移降为三分之一，松手单击；双指轻点进入右键/拖动菜单。普通轻点使用松手坐标，瞄准镜与画面共享旋转/缩放几何，取消和失控不落点击。参考依据和自定义方案边界见 DESIGN.md 的 3.0.12 记录。

Node 核心断言通过，覆盖瞄准松手单击、取消/失控/第二指接管不补点击。模拟浏览器完整回归通过：四入口布局、无需切换的滚动、按住不提前发令、30px/12px 移动对应 10px/4px 瞄准且松手只点一次。模拟结果不能代替手机软键盘、长按手感和真实小目标命中率验收。

已同步本机安装版 Web 资源并通过签名校验，HTTP 确认返回 3.0.12。安装后只读浏览器验证通过：真实 JPEG 解码 3 帧、四种视口四入口可见、退出停止画面；远端输入被测试拦截。

## 输入框始终可见（Web 3.0.13）

移除将空白/实时同步编辑区裁成 1px 的规则，保留可编辑文本框并与发送、关闭合成一行；文本框高 44–88px，长文使用原生内部滚动。全屏期间固定根页面、将首页隐藏并设为 inert，退出恢复原滚动位置；键盘动画跟随 VisualViewport，不反复强制页面滚回顶部。

模拟浏览器验证空白、实时替换、暂存和清空重建后编辑框可见；模拟可见视口高度 230/310/420px、偏移 0/65/120px 时输入框仍位于可见范围；既有同步、提交、手势与恢复回归通过。模拟软键盘视口不是 iPhone 原生键盘，Safari 动画及附件栏实际表现仍需真机验收。

已更新本机安装版并校验签名，HTTP 确认 3.0.13；安装后只读浏览器验证真实画面解码 23 帧、四种视口及退出停帧通过，测试未注入桌面输入。

## 发送暂停后的核验恢复（3.0.14）

首页/全屏不再因一次同步失败永久拦截发送，显式重试携带原草稿 ID/目标；电脑只在原控件内容核验通过后恢复，不清空重发。保留真实错误，重新取失败的全屏输入上下文，并使用本次请求租约代替首请求闭包。恢复边界与回归证据见 LIVE_INPUT_REVISION.md 的 3.0.14 记录。

3.0.14 已安装并重启，稳定签名校验通过，HTTP 返回新版脚本且辅助功能仍授权。只读浏览器真实解码 25 帧、四种视口及退出停帧通过，未注入真实桌面输入。旧二进制备份 /tmp/PocketDesk-before-retry-VoiceDeck。更新会结束旧的进程内草稿会话，旧页面中尚未发送的正文需先复制，再刷新开始新一轮；不得携旧确认基线静默重放。

## 已有正文及时同步（Web 3.0.15）

补齐手机已有草稿聚焦、进入全屏时的全文同步；候选结束补齐镜像，失焦不改变正文来源，提交读取当前面板全文。保持原草稿身份/目标和失败保护；不把电脑既有内容当作本轮手机草稿覆盖。

3.0.15 模拟浏览器回归通过，覆盖旧正文聚焦同步、全屏带入、追加时全文及同 ID、候选结束失焦后提交全文；已同步安装版 Web 文件、校验签名及 HTTP 版本，未重启 helper。真实手机到桌面的落字结果仍需验收。
