# 语音纠正：保留实时显示，取消逐字回删

2026-09-09 用户澄清：电脑输入框必须持续显示手机输入，不能为消除回删而把普通应用改成等待发送。本文是当前有效行为，覆盖 LIVE_INPUT_PROPOSAL.md 的重叠决策。

## 输入路径

- `selection`：通用应用默认。新增文字立即输入；修订保留公共前缀，选中本轮旧尾部，一次输入新尾部。不会先清空再重打。短文本使用 Unicode 事件，长文本/换行一次粘贴。用户主动删除时一次删除选区，文字纠正不发 Delete。
- `replace`：TextEdit 已保存的空白 `.txt` 文档可走 AXValue 整值替换，要求值/选区可读写、输入结果读回一致。
- `deferred`：仅保留 UU 特殊远控通道的发送时一次输入，不再作为普通应用默认。

手机持续上传最新全文，未发送快照可合并；发送只提交当前文字，不重复输入全文。不能把输入停顿当成识别最终结果。首页/全屏共用草稿 ID，图文使用同一提交请求。

## 边界与真实性

KeyboardDraftWriter 在能读取 AX 时记录原正文、选区和本轮插入起点。每次更新先验证预期全文/光标，保留本轮以外的前后文；优先一次设置 AXSelectedTextRange，然后通过编辑器正常输入事件写入，避免 AXValue 绕过网页模型。AX 选区不可写时使用 Shift+Left 扩展选区；已能读回的控件必须确认选区后才替换。

不提供文本/选区的编辑器只能沿用输入位置绑定及有序按键，不能检测所有同应用内移动光标、手动改写或丢键；该通道回执为 sent，不伪造 delivered。需要保持电脑输入位置不变，焦点/租约失效则暂停。对于大段改写，无法使用 AX 选区的控件仍可能短暂显示选区扩展，不保证所有应用无闪烁。

LiveDraft 固定一轮目标和模式；失败不自动重播；用户再次点发送可请求核验重试。近期 32 轮提交在当前进程内按 ID 去重；已同步会话丢失时不追加全文。不是跨进程 exactly-once 保证。提交动作发出不等于第三方消息已送达。

## 验证记录

- Swift 完整构建通过，原有 Server Any? 与并发捕获告警保留。
- 隔离状态机与选区写入测试通过：连续中文纠正、前后文保留、换行/组合字符/家庭 emoji、纠正零退格、主动清空只删除一次选区、焦点失效停手；原有整值替换/暂存/提交封闭测试通过。
- 模拟浏览器通过：selection 模式持续上传、草稿 ID 连续、修订发生在提交前；deferred 仅暂存；组合态不提前发送、图文提交、失败保留草稿及全屏回归通过。
- 已更新本机 PocketDesk。独立原生窗口测试因未取得测试窗口焦点而在输入前停止，没有把测试文字写入其他应用。ChatGPT、Chrome 等真实输入效果仍需在用户当前输入框验收；未将模拟测试宣称为真机通过。

成熟产品参考沿用 FULLSCREEN_WORKSPACE_PLAN.md 的 UU 官方键盘入口资料；公开材料没有证明其语音回改协议，不宣称复刻内部实现。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md

## iPhone 发送被暂停状态锁死（3.0.14）

用户截图确认命中 livePaused 前端守卫；本机 20:25–20:28 日志存在输入位置变化及替换结果无法确认，不能将此归因于 iPhone 候选事件。删除要求清空才能再发送的前端硬拦截，失败后保留原草稿 ID/目标和真实原因；再次发送附 retry，请求电脑核验。全屏重试重新获取上下文，避免永久等待之前失败的 Promise。

服务端仍校验原会话/控件/前后文和本次租约；原文已完整落入只更新基线后提交，未落键且基线正确才允许继续写入。发过写入但仍读到旧值不证明未落字，禁止据此重放。内容不一致、未知控件已发令但结果不明、最终图文提交阶段停止均不自动恢复。修复 KeyboardDraftWriter 永久捕获第一条请求旧控制 session 的问题。

隔离 Swift 测试验证恢复不重复输入、写前失败恢复、旧读数/未知结果/内容冲突/最终提交停止仍拒绝；模拟浏览器验证首页和全屏失败后可显式重试且保留原 ID、正文，暂停不自动重放。实际飞书/iPhone 输入仍需真机验收。

3.0.14 已安装并重启，稳定签名校验通过，HTTP 返回新版脚本且辅助功能仍授权。只读浏览器真实解码 25 帧、四种视口及退出停帧通过，未注入真实桌面输入。旧二进制备份 /tmp/PocketDesk-before-retry-VoiceDeck。更新会结束旧的进程内草稿会话，旧页面中尚未发送的正文需先复制，再刷新开始新一轮；不得携旧确认基线静默重放。

## 已有手机草稿接续（Web 3.0.15）

隔离复现：手机框程序带入旧正文后聚焦，300ms 内没有同步请求；新增文字后才请求，但请求确实包含完整正文。因此已确认的是启动同步的缺口，不能据此断言电脑端只收到了增量。

已有非空正文聚焦、进入全屏输入栏时即进入原有全文同步队列；保留原草稿 ID 和目标，不重置电脑基线。当前编辑面板决定正文来源，不因 iOS 键盘失焦改读旧镜像；compositionend 补齐首页镜像，提交读取当前面板的全文。失败暂停和特殊远控暂存仍不自动重放。此修复是手机到电脑的草稿接续，不读取/覆盖电脑原有正文。

3.0.15 模拟浏览器回归通过，覆盖旧正文聚焦同步、全屏带入、追加时全文及同 ID、候选结束失焦后提交全文；已同步安装版 Web 文件、校验签名及 HTTP 版本，未重启 helper。真实手机到桌面的落字结果仍需验收。

## 首页首字后停止：绑定生命周期排查（2026-09-10）

用户截图显示首页正文完整、目标 ChatGPT，错误为“输入位置已变化”。运行日志同时包含替换结果无法确认；尚不能将所有失败归为同一原因。检查发现 applyDraft 每次快照调用 establish：AX 从应用级变为元素级、或探测能力暂变时会换发上下文，使旧 LiveDraft 与新令牌不一致。修正为同一草稿 ID 沿用原上下文，仅新草稿建立绑定；原目标、请求上下文、租约、原绑定和文本/选区校验继续生效。

选区写入失败新增既有日志通道的 live-diagnostic，记录失败阶段、文本相等关系、UTF-16 长度与选区，绝不记录正文。此诊断用于区分后续真实焦点变化和读回不一致，不据此自动重放。Swift 全量编译与输入事务隔离测试通过；用户原手机到 ChatGPT 的路径仍待复测。

修复已安装并重启：稳定签名验证通过，HTTP 正常返回，辅助功能仍为已授权；安装时保留原 Web 与配置。旧二进制仅保留一份 /tmp/PocketDesk-before-input-binding-VoiceDeck，真机首字后连续追加通过后删除。重启前的进程内草稿无法恢复，复测前应保留手机草稿并刷新页面，在空白测试框重新开始。

## 删除残留与已有手机正文（Web 3.0.17，2026-09-10）

用户确认保留电脑原文，手机只管理自己这一段。隔离浏览器复现同步暂停后删空不发请求：旧代码丢弃草稿身份，导致电脑残文脱离本轮管理。现在删空立即上传空串且保持 ID，暂停后的显式删除携 retry 先核验；前次失败回执晚到时，队列仅保留同草稿、同目标、同上下文已排队的删除，不跨目标、不重放失败的写入。核验期间新增文字在恢复后补齐。

手机正文聚焦、进入全屏、页面返回及首次目标就绪时同步全文，包含已有内容。移除全屏入口把电脑原文反填到手机的逻辑，避免随后重复追加；手机删除范围保持为本轮文本，电脑原文仍由 KeyboardDraftWriter 的前后文边界保护。

浏览器回归与队列测试通过。运行日志另有 textEqual=false、utf16=1/6、光标一致的读回异常，尚不能确定是否为空框占位或应用 AX 不一致；只读焦点诊断增加字符数量、值长度、占位文本相等关系，不记录正文，不放宽冲突保护。

3.0.17 已安装：HTTP 返回三个新版脚本，安装文件与工作区一致，稳定签名和辅助功能授权正常。浏览器 32 次模拟写入、队列迟到失败回归、Swift 原文边界内连续删除/再输入通过。备份统一为 /tmp/PocketDesk-input-sync-backup（旧二进制及本次修改的四个 Web 文件），替代上次单文件备份；真机验收通过后删除。当前真实前台已切到钉钉，ChatGPT 的读回异常等待原输入框复测，不能宣称已解决。

3.0.22 待安装验证：Chrome 多图锚点改由当前 PID、聚焦窗口、鼠标命中与焦点所在 WebArea 的页面矩形共同确认，不再依赖 Chromium 会重建的 AX 节点身份；工具栏、后台窗口和其他应用仍拒绝。应用切走再切回后，用户再次发送会以当前焦点继续暂停前已实时输入的正文，只执行尚未开始的图片与提交动作，不把手机全文再注入一次。纯逻辑与编译验证不能替代飞书网页真机验收。

## 电脑已提交但手机仍留字（Web 3.0.18）

已隔离复现：普通回车快捷键直接调用 shortcut-trigger，电脑收到回车但手机草稿未封闭；另一个路径是提交回执成功后先写本地历史，存储异常会跳过清空。修复为有手机草稿的普通回车复用 send；成功收尾先清空并重建编辑会话，历史保存独立容错。旧元素迟到的 input/compositionend 不再影响新草稿，避免已发送文字被输入法回填。空框回车和带修饰键的快捷键保留原行为，真实提交失败仍保留正文。

本次工作期间另有多图片改动同时写入 Web/app.js、compose.js、index.html 与服务端；本次验证/发布基于已安装 3.0.17 的隔离副本 /tmp/PocketDesk-submit-fix，只加入提交收尾修复，不发布未验收图片功能。相同修复也保留在工作区供后续集成。工作区图片代码的 HTTP randomUUID 兼容问题属于该并行改动，不能用本次隔离回归宣称整个工作区通过。

隔离回归通过（34 次模拟写入），覆盖回车提交、旧 IME 事件、历史配额失败及既有删除/重试/全屏路径。3.0.18 网页已安装，HTTP 版本和安装文件逐字比对通过、签名有效，未重启 helper。临时验证副本已删除；/tmp/PocketDesk-input-sync-backup 更新为此次安装前的完整回退所需文件，手机真机验收通过后删除。
