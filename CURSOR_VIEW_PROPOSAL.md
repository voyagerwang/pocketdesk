# PocketDesk 画面与鼠标反馈：执行交接方案

> 2026-09-08；基于提交 57613f1 的代码阅读与 Apple/MDN 文档评估。本文是待执行方案，性能数字是目标，不是实测结果。
> 本轮按用户要求停止功能实现，只整理交接；接手者必须先核对当前 Git diff、CLAUDE.md 和文件头，不覆盖其他模型的新改动。

## 1. 结论与范围

能展示鼠标，也能让它比桌面截图更新得更快。

现有 ScreenCapture.swift 已设置 showsCursor=true，JPEG 内带有捕获时的鼠标；screen.js 的设计刷新间隔为上一帧完成后再等一秒。因此现在只能看到离散位置，不能称为实时移动。

建议第一阶段：低频桌面截图 + 独立鼠标位置回传 + 手机叠加指针。Mac 每秒最多采样约 30 次真实指针位置，变化时发送；手机通过 requestAnimationFrame 绘制最新状态。初版不做位置预测，不做通用鼠标形状同步。

它适合看回复、定位光标、轻量点击。鼠标流畅不代表窗口、菜单、拖拽对象流畅；需要连续拖动反馈时，另立第二阶段升级画面传输。

## 2. 产品交互

### 主界面

- 输入和触控板继续作为主要操作区域。
- 画面能力按需打开，主入口尽量统一为「画面」，不要平铺全屏、悬浮、刷新、帧率等设置。
- 打开后可专注放大阅读，也可以选择「边看边操作」进入悬浮小窗；不要自动弹出画面打断输入。
- 现有代码已加入全屏和 PiP 悬浮窗，应复用其生命周期，不另写一套轮询。

### 看与控制的分工

- 大画面用于阅读；原生 modal dialog 会阻止底层触控板操作，不能假设打开大画面后还能操作主界面。
- 第一阶段通过现有悬浮窗配合主触控板，实现边看鼠标边移动。精细操作可后续在全屏内提供可收起触控板，复用手势模块，不复制第二套状态机。
- 建议统一图片点击语义：点画面只定位，单独明确执行点击；当前全屏只定位、悬浮窗直接点击的差异容易误触，尤其小窗中文字很小。
- 放大、平移属于查看操作，不能同时发送电脑点击；必须有移动阈值、pointercancel 和拖动结束抑制 click。
- 返回保留输入草稿、目标和页面位置；关闭最后一个画面视图或切后台即取消鼠标订阅。
- 查看器「暂停更新」冻结截图与叠加指针，显示暂停状态，避免活鼠标叠在旧画面上造成实时错觉。
- 显示「鼠标在另一块屏幕」及可切屏动作，不自动切屏打断阅读；无效屏幕 ID 不允许回退主屏后执行点击。

## 3. 现有代码卡点：先修，再叠加

| 位置 | 问题与影响 | 建议处理 |
|---|---|---|
| ScreenCapture.swift | 单次截图带鼠标；新增叠加层会出现两个鼠标 | 在独立鼠标功能启用的画面请求中使用 showsCursor=false，明确单一渲染路径；旧客户端仍需可用，不能先全局关鼠标却没有回传 |
| PointerExecutor.currentPosition | position 首次读取后一直用缓存，实体鼠标改变位置不会自动同步；手机下一次移动/点击可能跳回旧位置 | 增加独立真实坐标采样；区分观测值和命令期望值。执行队列顺序应用指令，突发命令保留累积，采样用于校正；验证注入后系统读数的时序，避免用滞后观测覆盖刚提交的移动 |
| PointerExecutor.clamped | 所有屏幕的外接矩形包括 L 形排列空洞，maxX/maxY 边缘也可能落在范围外 | 基于有效显示器矩形集合限制位置；越界投影至最近有效边缘；处理负坐标、镜像和拔屏 |
| PointerExecutor.displayBounds / tap | 无效显示器 ID 回退主屏可能把点击送到错误应用 | 操作类命令对失效 ID 返回明确错误；刷新拓扑后由用户重新选择；校验坐标有限值与范围 |
| Web/screen.js sendTap | 使用整个 img DOM 矩形映射；object-fit:contain 会留下空白，空白也算进显示器坐标 | 提取纯坐标转换函数，依据真实图像内容矩形映射；黑边/留白不接受点击。放大、滚动、横竖屏共用此变换 |
| Web/screen.js sendTap / app.js | 发出指令后立刻写“光标已移至”，但没有服务端确认；wsReady 在 onopen 就为 true | 增加 auth_ok、command ack 和错误回复；“已发送”与“执行器已处理”分开；ack 不表示目标应用完成点击效果 |
| Web/screen.js capture | 当前 finally 用 !timer 决定下一轮；clearTimeout(timer) 后没清空句柄，自动更新可能跑过一轮后停住 | 定时器回调及清理统一将句柄置空，保持单一调度入口；以持续至少 10 秒验证，不能只看第一帧 |
| WSServer | 当前主要负责接收，没有已认证订阅者列表、鼠标回传和完整断线收尾 | 增加订阅/退订与连接清理；发送只对已认证连接；disconnect/timeout 释放该控制会话的拖动状态 |

以上是静态代码评估。根因修复应由执行模型补充最小复现；不要把所有潜在症状描述为已经在用户机器上发生。

## 4. 推荐技术路径

数据流：

    Mac 实际鼠标位置 → CursorMonitor → 已鉴权 WebSocket → 手机 cursor overlay
    Mac 桌面 → ScreenCapture → JPEG → 手机 image
    手机触控板 → 原有 WebSocket 命令 → PointerExecutor → Mac 输入事件

### 4.1 Mac 指针采集

新增职责单一的 CursorMonitor（建议名称，不要求机械照搬）：仅在有可见画面订阅者时，以约 30Hz 定时读取 CGEvent(source:nil)?.location，并对照 CGDisplayBounds 计算显示器归属。

优先做采样方案，验证实体鼠标与 CGEvent 注入都能跟随；不一开始引入全局事件 tap 及其权限、回调超时和 run loop 维护成本。若实际测量发现短暂移动捕捉不足，再评估只监听事件的方案。

不同线程禁止随意读写 PointerExecutor.position。采集器维护观测状态，输入执行器维护指令顺序，通过明确的消息或串行队列边界协调。鼠标位置报告不等于命令执行回执。

### 4.2 回传协议（建议草案）

沿用 :46388 的配对鉴权和双向 WebSocket。无订阅时不推送，订阅须在 auth_ok 之后建立。

客户端：

    {"t":"cursor-subscribe","enabled":true}

服务端：

    {"t":"cursor","seq":123,"displayId":1,"rx":0.42,"ry":0.61,"topologyVersion":2,"observedAtMs":45678}

其中 rx/ry 是该显示器内的归一化坐标；displayId 无有效归属时可以为 null；observedAtMs 为服务端单调时间，不允许直接与手机 Date.now 相减。

拓扑消息应包含各屏幕的全局逻辑矩形、显示器 ID、版本。截图请求/响应应能对应同一显示器与拓扑版本，并携带 captureId；收到拓扑变更立即隐藏旧叠加，重新取图后再显示。

新协议必须兼容旧客户端：保留现有命令，未知消息可安全忽略。引入版本/能力协商，明确截图是否含鼠标。若要添加 commandId/ack，用独立字段表示执行器已处理，不借用 cursor seq 假装输入确认。

### 4.3 手机绘制与坐标换算

鼠标作为 image 上的独立 DOM/SVG 层，用 transform 更新位置，pointer-events:none，不截获手势；轮询截图更换 Blob 时不要重建叠加层。

给定图片自然尺寸 W/H，容器可用尺寸 Cw/Ch：

    scale = min(Cw/W, Ch/H)
    contentWidth = W * scale
    contentHeight = H * scale
    offsetX = (Cw - contentWidth) / 2
    offsetY = (Ch - contentHeight) / 2
    cursorX = offsetX + rx * contentWidth
    cursorY = offsetY + ry * contentHeight

必须按实际 object-position、缩放和平移扩展公式；放大时不能继续使用适应模式的 scale。最好让图片和叠加层共用一份几何状态，再从同一状态反算点击坐标。

DOM 坐标用 CSS 像素；不直接将设备 devicePixelRatio 乘进输入坐标。若改用 Canvas，DPR 只用于绘图 backing store。光标热点固定在箭头尖端，不能拿图标中心对准目标。

第一次同步先用真实报告位置直接绘制；每次 rAF 只取最新状态。不绘制未确认预测位置，不发送根据动画插值制造的坐标。

### 4.4 形状和双鼠标

第一阶段只保证位置，使用清晰的固定箭头，允许其与系统 I-beam、手形、缩放箭头不同。这个叠加层是位置指示器，不声称复刻系统当前鼠标形状或可见性。

跨进程获取实时光标图像、热点和隐藏状态不能假设 NSCursor.current 就代表其他应用；必须单独调研、验证。若后续要求形状与画面严格一致，可考虑保留原生鼠标的视频画面，或独立的鼠标形状协议。

叠加显示时截图必须去掉烘焙鼠标。坐标通道断开时隐藏叠加并说明连接状态，不能在最后一个位置继续冒充实时；需要降级时显式切回含鼠标截图。

## 5. 性能与故障评估

| 卡点 | 可能问题 | 对应方案 |
|---|---|---|
| 一秒截图 | 指针在移动，窗口/菜单仍在旧位置；按旧图点击会落错对象 | UI 显示截图新鲜度；点击/滚动后可触发去重的提前刷新；拖拽验收不以此架构宣称成功，必要时升级视频 |
| WebSocket 是 TCP | 网络拥塞时旧状态排队，出现延迟越来越大 | 小包专用；同一连接最多一个在途 cursor send，再保留一个最新待发状态。浏览器检查 bufferedAmount，服务端限制待发量；图片不塞进控制通道 |
| 丢弃策略 | 丢相对 move 增量会少走距离，丢 up 会卡住拖动 | cursor 状态可覆盖；相对移动要累积合并；down/up/click 保序不可任意丢弃，点击前刷新待发移动。断线重连清空旧命令，不回放旧点击 |
| 客户端预测 | 看似顺滑但与 Mac 实际位置不一致，校正跳动 | 第一阶段不预测，先测真实回传。后续预测须有 commandId、确认和校正，并明确显示预测与真实的差别 |
| 实体鼠标与手机同时操作 | 两个控制源抢位置，缓存跳变 | 接受真实位置为观测基准；定义控制权策略，至少限制一个手机控制会话，多手机可只读。实体鼠标移动后不把光标拉回旧缓存 |
| 截图/坐标时间不同步 | 屏幕换了、叠加仍对应旧坐标系 | displayId + topologyVersion + captureId；几何不匹配不画光标。时间字段仅用于排序/测量，不假设两台设备时钟同步 |
| 多屏/Retina/旋转 | 放大后偏移、负坐标出错、鼠标越屏消失 | 全局逻辑坐标转换为屏内比例，再映射到图片内容矩形；跨屏给提示；更新时测试拔屏、不同缩放和旋转 |
| 手机后台与重连 | 定时器暂停、旧图残留、回到页面重放动作 | visibility/pagehide 退订并取消请求，恢复重新鉴权、取图、订阅；旧 seq 与旧 epoch 状态作废 |
| 权限/锁屏/休眠 | 有输入权限但没录屏权限；画面被冻结而仍可发点击 | 分开检查辅助功能与录屏；锁屏/休眠/超时明确不可用；不承诺后台解锁。图像不新鲜时提示并限制直接画面点击 |
| 资源消耗 | 长时间轮询/编码发热、耗电、内存增长 | 可见订阅才采样，位置不变不发（低频心跳），订阅数为零停计时器；回收 Blob，检查慢客户端内存是否稳定 |

30Hz 的每包若约 150 字节，正文约 4.5KB/s；这只是算术估算，不含协议、TCP 和重传。位置数据通常远小于图像，但不等于网络延迟已达标。

## 6. 什么时候升级画面

若用户只想知道鼠标在哪里，完成第一阶段即可。

若用户需要边看边拖窗口、选中文本、频繁滚动，必须提高内容画面更新率：

1. 改持续 SCStream 捕获，在有观看者时启动，退出后停止；先评估 10–15fps 的压缩帧实验，避免把单帧截图 API 生硬调用到 30/60 次每秒。
2. 若 JPEG 的带宽/编码成本不达标，再评估 VideoToolbox 硬件编码 + WebRTC 视频。完整链路涉及编码器、信令、ICE、浏览器解码、生命周期与网络安全；同一局域网可简化网络路径，但不会自动免除这些工作。
3. 无论选择哪种传输，都不能靠无限加大队列换“流畅”；过期画面应丢弃，优先新鲜帧。

延迟拆开测：手势发送、Mac 应用指令、真实位置采样、回传、手机呈现。另测“点击到目标界面变化可见”的内容延迟。两者不能拿同一个指标代替。

## 7. 验收与执行顺序

### P0：建立可靠基础

- 先重现/修复自动更新定时器、含留白图片点按映射、无效显示器 ID、实体鼠标与缓存同步。
- 检查点击之前的待发送 move 是否保序、断线拖动是否释放。
- 保留当前全屏/PiP、输入和配对功能；对新协议做兼容处理。

### P1：独立鼠标状态

- 已鉴权订阅 → 真实位置采样 → latest-only 状态推送 → 两种画面共用的叠加层。
- 先固定箭头，关闭该路径截图中的鼠标，验证只出现一个指针。
- 关闭视图、暂停、后台、跨屏、重连状态全部有明确行为。

### P2：按实测决定视频升级

- 若位置反馈达标而拖动反馈不达标，结论是画面链路需要升级，不是继续加鼠标动画。

验收矩阵：

- Android Chrome、iPhone Safari 实机；窄竖屏、横屏；不能把桌面模拟器当作实机验收。
- 手机触控板移动/点击/拖动，实体鼠标移动，两者交替；两个已配对手机的控制策略。
- 主屏、副屏、负坐标、Retina 混合缩放、显示器拔插、镜像与旋转。
- 适应屏幕/原尺寸/滚动后的四角与中心点一致；留白点击无效；截图与指针没有双影。
- 请求持续至少 10 秒不中断；持续 10 分钟无队列/内存增长；后台无截图和高频鼠标推送。
- 拔网线/关闭 Wi-Fi/权限撤销/锁屏后不把旧图和旧光标称为实时；重连不执行之前的点击。
- 所有测试用无破坏性的测试窗口，不点击用户工作内容做验收。

性能目标：局域网中“手机动作到权威光标在手机呈现”争取 P95 <150ms，实体鼠标移动到手机呈现采用相同目标；这是首轮工程目标，必须以实测报告说明是否达到。用客户端往返/ack 计时作诊断；最终可用同时拍到手部动作、电脑、手机的高帧率录像测感知延迟，不能直接相减未同步的设备时间。

交付应包含：改动文件、L1/L2/L3 文档同步、兼容说明、真实测量条件与结果、剩余限制。没有安卓/iPhone 实机时直接标明未验证，不虚报通过。

## 8. 给执行模型的指令

请先读取本方案与当前代码地图，核对 Git 状态及当前提交。只实施 P0 和 P1，P2 留作评估；不引入云服务、不重写输入协议、不覆盖其他模型改动。先修坐标与状态真实性，再做鼠标显示的流畅性。以“按需看画面、边看边控制、没有虚假实时反馈”为验收目标。给出可复现测试结果，未经实测的性能数字必须标为目标。

## 9. 参考依据

- Apple showsCursor：决定是否把鼠标显示在捕获流中；现有代码已开启。[官方文档](https://developer.apple.com/documentation/screencapturekit/scstreamconfiguration/showscursor)
- Apple CGEvent.location：事件位置采用全局显示坐标。[官方文档](https://developer.apple.com/documentation/coregraphics/cgevent/location)
- Apple minimumFrameInterval：控制持续流的最小帧间隔。[官方文档](https://developer.apple.com/documentation/screencapturekit/scstreamconfiguration/minimumframeinterval)
- Apple ScreenCaptureKit 捕获与权限。[官方示例](https://developer.apple.com/documentation/screencapturekit/capturing-screen-content-in-macos)
- MDN WebSocket：普通 API 没有自动背压，需要显式限制缓冲。[API 文档](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket) / [bufferedAmount](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket/bufferedAmount)
- MDN WebRTC：实时媒体、数据通道与信令基础。[官方文档](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API)

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
