# PocketDesk 手机体感鼠标调研与执行建议

> 2026-09-08。调研与方案，不代表已实现或完成手机实测。与 CURSOR_VIEW_PROPOSAL.md 的鼠标回传方案配套。产品事实、平台约束与本项目设计建议分别说明。

## 1. 结论

可以用手机陀螺仪实现空中鼠标，已有商用先例。推荐“旋转手机产生鼠标相对位移”，不要承诺“手机在空间平移多少厘米，光标就精确移动多少距离”。陀螺仪测量角速度；仅靠手机惯性传感器推算平移距离需要处理重力、姿态误差与二次积分漂移，不适合作为本项目第一版定位方式。

最佳实践不是所有动作都体感化，而是体感负责移动，触摸负责点击和滚动，并能随时暂停/重新握持。这里的“最佳”是基于成熟产品规律做出的项目建议，不是所有设备和场景均适用的统一标准。

尤其注意本项目场景：人在另一房间，看的是同一部手机上的电脑画面。转动手机会同时转动画面，可能比触控板更难阅读和瞄准。体感适合作为可选模式：看远处显示器、大按钮和短时操作；看手机长回复、精确选字时保留触控板为默认。

## 2. 成熟先例与可借鉴点

- Remote Mouse 官方提供 Gyroscope Mouse，使用手机陀螺仪/加速度计控制光标；同时保留触控板和其他输入。说明手机方案已商业化，不能由此推断所有手机、浏览器或本项目手感都已验证。[官方产品页](https://www.remotemouse.net/)
- LG Magic Remote 官方开发说明支持 Pointer 与方向键模式，使用滚轮上下滚动。可借鉴“指向与滚动分离”和保留传统控制备选；这不是手机 Web API 可直接复制其硬件体验的证明。[LG 开发文档](https://webostv.developer.lge.com/develop/guides/magic-remote)
- Gyration Air Mouse 厂商用户手册使用按住触发器激活移动、释放后定位点击的策略；双击激活扳机用于锁定移动模式，不要误解成向电脑发送鼠标双击。[厂商手册镜像](https://gzhls.at/blob/ldb/2/a/d/7/51589fd9a859d4998c635df593fc34c14d56.pdf)

没有找到可据此断言“摇手机双击、倾斜滚动是最佳默认”的证据。本方案不采用这两种默认行为。

## 3. 推荐动作映射

| 用户意图 | 第一版交互 | 设计理由 |
|---|---|---|
| 移动鼠标 | 按住大面积「按住移动」区，小幅左右/上下转动手腕 | 显式启停，换姿势、说话、把手机放下不会一直移动鼠标 |
| 暂停/重新握持 | 松手即停；调整握姿，再按住继续 | 相当于鼠标拿起重新放下，重设相对姿态基准，不把光标强行回中 |
| 单击 | 松开移动区，再点「左键」 | 点击时不再由体感改变目标位置，减小按屏引起的晃动 |
| 双击 | 点一次「双击」按钮，由 Mac 执行两次点击 | 无需悬空握持还精确连点；两次点击固定位置和顺序 |
| 右键 | 明确的「右键」按钮 | 不复用晃动或难发现的长按手势 |
| 上下滚动 | 拇指在独立竖向滚动条滑动；或沿用触控板双指滚动 | 不与上下移动鼠标争用同一个体感动作 |
| 精细调整 | 切回现有触控板；可在后续增加慢速档 | 手腕体感不必承担精确选字等任务 |
| 拖拽 | 第一版可继续用原触控板；若加入体感拖拽，用明确「按下/释放」状态 | 切模式、退出、断线必须抬起左键，不能靠猜测长按结束 |

手机一只手使用时优先给滚动条和移动区足够拇指触达面积；不要要求同时按住移动区与另一个点击按钮。更熟练的用户可后续选择常开体感，但默认仍采用按住激活。

进阶“按住滚动键 + 前后倾斜”可以实验，需独占滚动模式、冻结鼠标位置、死区、速度上限和松手立即停止。不能一边俯仰移动光标，一边又把相同动作解释为滚动。

## 4. 双击的实现边界

当前 Sources/PointerExecutor.swift 支持 click + count，并使用 mouseEventClickState。因此明确双击按钮可复用：

    {"t":"click","button":"left","count":2}

但执行模型需核对当前语义：这是完整的双击序列，不是“第二下鼠标事件”。如果第一下已经发送 count:1，第二下又发送 count:2，就可能产生三次按下/抬起。不要简单复用现有触控板双击识别而不验证事件总数。

推荐第一版独立双击按钮一次发送完整命令，服务端在同一位置依次执行 clickState 1、2。执行之前保证待发 move 已入队，执行期间不插入陀螺仪移动。时间应兼容 macOS 双击设置，分别验证 Finder、浏览器、编辑器，不能只看日志里有两次事件。

若未来允许连点左键识别双击，再单独决定延迟单击判定还是原生按次传递的语义，不让两种方案混用。

## 5. Web 接入：最大的前置卡点

### 安全上下文与权限

DeviceOrientation / DeviceMotion 相关能力受安全上下文和浏览器权限限制。当前手机访问 http://<Mac>.local 或 http://192.168.x.x，不应作为跨平台可靠的传感器运行环境。手机端的 localhost 指手机自身，不能借用电脑 localhost 的开发例外。

应先获得可信 HTTPS；根据运行环境检查 isSecureContext、接口存在性、requestPermission 方法是否存在以及是否收到有效传感器事件。接口存在不代表有陀螺仪，也不代表授权成功。

某些浏览器（尤其需要显式请求的实现）要求在用户点击处理里调用 requestPermission；不能页面一打开就请求，也不要先等待无关网络请求而丢失用户激活。方法不存在时继续按该浏览器流程监听并做超时检测，不直接判定为不支持。

资料：[MDN requestPermission](https://developer.mozilla.org/en-US/docs/Web/API/DeviceOrientationEvent/requestPermission_static) / [W3C 规范](https://www.w3.org/TR/orientation-event/) / [MDN 安全上下文](https://developer.mozilla.org/en-US/docs/Web/Security/Secure_Contexts)

### HTTPS 与 WebSocket 要一起改

当前 Web/app.js 硬编码 ws://。改 HTTPS 后需要 WSS，不能只让 HTML 变成 HTTPS、鼠标仍连接明文 WS。推荐统一同源入口，例如 HTTPS / 与 WSS /ws，反向代理分别转到 HTTP :46387 和 WS :46388；校验代理确实支持 WebSocket Upgrade。

特别注意现有后端对 loopback 连接豁免鉴权。反向代理从本机转发时，远端手机可能在后端看来全部是 loopback。必须显式配置代理入口也校验配对 token，不能仅靠连接源地址推断机主，也不能信任客户端任意伪造的转发头。

二维码地址和 origin 变化后，localStorage 中的配对 token 不会自动迁移，需通过新地址重新扫码。若仅验证传感器，不要把生产配对信息发送给公共演示站。

### 三条部署路线

1. 已用 Tailscale 的个人实验：评估 Tailscale Serve 的私网 HTTPS；官方支持自动证书。仍需处理 WSS 路由和上述 loopback 鉴权问题。它依赖 Tailscale 账户/网络/证书能力，不是零外部依赖的纯离线局域网方案。[Serve 文档](https://tailscale.com/docs/features/tailscale-serve)
2. 坚持纯局域网网页：本地 HTTPS 服务 + 手机信任的证书，或拥有域名及受信证书并做局域网解析。证书分发、续期和信任安装是产品成本；只点过无效证书警告不能作为正式兼容方案。
3. 原生手机端：直接用 iOS Core Motion / Android Sensor API，保留现有桌面执行协议。传感器控制空间更大，但引入手机应用开发、分发和维护；PWA 安装本身不能绕过 Web 安全要求。

## 6. 数据处理与手感

推荐：角速度或相对姿态变化 → 坐标系转换 → 去偏置/滤波 → 速度增益 → 相对 dx/dy → 已有 WebSocket move。

- 不做加速度二次积分求手机平移距离。
- 转动停止时光标停止。不要默认做“手机一直倾斜，鼠标就一直走”的摇杆模式。
- rotationRate 是按时间计量的数据，使用事件时间戳计算 dt；Web 与原生 Android 的单位不同，不能混用 degrees/s 与 radians/s。
- 对横竖屏、握持方向和手机绕自身长轴的旋转做坐标变换；不要固定把 beta 映射到 Y 就宣称所有握姿都正确。
- 如用姿态角差，要处理角度跨 360 度的跳变；宽姿态范围应考虑旋转矩阵/四元数。初版可明确支持竖屏自然握持，旋转时暂停并重设基准。
- 每次重新激活丢弃暂停期间的传感器累积；后台恢复清空时间基准，防止 dt 变大导致光标飞走。
- 静止校准估计零偏，只在确实静止时更新，不能把缓慢有意移动学习成噪声。
- 滤波在防抖和延迟之间取舍。可评估 1€ Filter：慢速时压抖动、快速时降低迟滞；它不是漂移校正器，参数必须实测。[作者算法与调参说明](https://gery.casiez.net/1euro/)
- 速度增益建议慢动作便于精确瞄准、快动作便于跨屏；保留简单的灵敏度选项，不暴露一堆滤波参数。
- 在手机侧处理传感器，向 Mac 发合并后的 dx/dy；不会因为设备每秒产生很多事件就逐个发包。相对位移合并时累加，不能只保留最后一个增量。
- 输入延迟与鼠标回传延迟分开测试。空中鼠标只解决输入，不能解决一秒截图带来的视觉延迟；结合 CURSOR_VIEW_PROPOSAL.md 的独立指针回传。

Android 官方说明陀螺仪测角速度且并非所有设备具有同样的传感器组合，必须能力探测。[Android Motion Sensors](https://developer.android.com/develop/sensors-and-location/sensors/sensors_motion)

## 7. 关键风险与处理

| 风险 | 处理 |
|---|---|
| 用户拿着手机看手机画面，同时转动它 | 作为可选功能；用小幅手腕运动，保留触控板默认；试验必须包括离开电脑的真实场景 |
| 点击时手抖导致点错 | 松开体感移动后点击；冻结的是输入移动，不是伪造画面上的实际鼠标位置 |
| 滚动时指针漂移到别的窗口 | 滚动模式冻结体感指针输入，保持实际光标位于要滚动的目标区域 |
| 双击生成三次点击 | 明确区分完整双击命令与单个点击事件；单测实际事件序列，做无破坏性 UI 验收 |
| 切回语音输入仍在控制鼠标 | 输入框聚焦、关闭体感模式、页面隐藏、pointercancel 都立即停止体感 |
| 设备无陀螺仪或浏览器不给数据 | 显示具体原因并回退触控板；不能只判断浏览器名称 |
| 传感器频率波动、断网 | 时间归一、异常 dt 重置、断线停止、重连不回放旧位移与点击 |
| 追求顺滑导致滤波过强 | 分开测静止漂移、微调、快速跨屏、点击命中率；以任务完成效果裁决 |
| 体感疲劳 | 短时模式、手肘可支撑、无需大幅挥臂；保留全部传统操作 |

## 8. 最小实验与验收

先验证可信 HTTPS + WSS + 传感器实际数据，再做移动手感，最后加入按键和滚动。不要先画完整界面，最后才发现手机 HTTP 页面无法拿到数据。

首版范围：显式体感入口、按住移动/松手暂停、左键、明确双击、右键、拇指滚动条、触控板回退。不做摇手机识别点击、惯性平移定位、长时间姿态锁定、复杂自定义手势。

测试 Android Chrome 和 iPhone Safari 实机，至少包括：静置不漂、慢速瞄准、快速转向、按键不跑偏、双击只生成两次点击、滚动不移动鼠标、重新握持不跳变、后台恢复不飞走、断线拖动释放、拒绝授权回退。

对比同一组任务的触控板与体感：定位小按钮、双击打开测试对象、滚动阅读并追问；记录完成时间、误点次数、主观疲劳和看手机时的可读性。产品取舍以此结果决定，不能把“有传感器事件”或动画很炫作为成功判据。

首轮不承诺优于触控板。若需要向其他模型下达任务：只先完成传感器连通性与按住移动原型，报告设备/浏览器/证书/权限条件、手感与误差，再决定是否进入完整交互实现。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
