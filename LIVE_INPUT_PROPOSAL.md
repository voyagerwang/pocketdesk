# PocketDesk 手机草稿实时同步方案

> 2026-09-08；调研与执行交接，不代表功能已实现或已完成目标应用实测。
> 目标：手机输入框里的文字持续出现在选定 Mac 应用的输入框中，支持后续修订，用户明确提交前不发送消息。
> 本文只规划本功能，不要求执行者实现陀螺仪切应用、语音识别服务或重新设计界面。

## 1. 结论与体验边界

可以实现，但不能承诺任意应用、任意输入框、任意手机输入法都能逐字同步。

推荐先做「单向、会话绑定、可验证的草稿同步」：手机是会话期间的编辑源，Mac 的一个具体纯文本输入框是镜像。手机增删改，Mac 跟随；Mac 被人编辑或焦点变化，立即暂停。第一版不做双向合并。

两段链路要分开：

1. 手机输入法/语音识别 → 网页 textarea：网页只能获得输入法实际交付的文字事件。若语音输入法说完一整句才提交，Mac 也只能整句出现。网页无法读取另一个输入法内部的候选或识别流。
2. 网页 textarea → Mac 输入框：这部分由 PocketDesk 控制，可以持续同步文字快照，并处理语音回改、删除和中间插入。

「像豆包」指少等待、少复制、文字持续落到工作位置的体验，不宣称复刻其内部实现。豆包官网介绍语音识别、纠错和文字整理，但未公开足以确认其跨应用写入和中间结果协议的技术资料。[1]

首版名称建议「实时同步」，说明「输入时同步到电脑，点发送才提交」。不加假打字动画，不人为把一句话逐字回放。速度目标从网页收到文字开始计时，不从人开始讲话计时。

## 2. 当前代码事实与必须避开的复用陷阱

执行前重新读最新 CLAUDE.md、Sources/CLAUDE.md、Web/CLAUDE.md 和相关文件头；项目可能被其他模型继续修改，以代码为准。

- Web/app.js 的 send() 读取 trim() 后的全文，POST /api/send，成功后清空输入框并进历史。它是一次性发送，不是草稿同步。
- InputExecutor.dispatchSend → performPaste 会注入全文，随后无条件 postKey(36) 按 Return。不得用高频调用 /api/send 实现实时输入，也不得在同步模式最后再发送一次全文。
- ensureEditableFocus 是尽力寻找可输入元素的现有逻辑；它返回 Bool，没有锁定一个确定的输入框。实时覆盖不能每次 BFS 找到哪个就写哪个。
- finishSend 当前根据前台应用与可输入焦点推断 delivered；这不足以证明草稿内容相同。新链路必须读回实际文字核验，不沿用这种成功判据。
- UU 是剪贴板跨机器特殊通道，本机无法读回远端目标框；首版明确不支持实时同步。
- WSServer 下行 enqueue 使用一个 latest-only pending 槽，光标帧可覆盖先前消息。同步确认、错误、会话结束不能接入这个可覆盖槽。
- 前端会自动跟随 Mac 前台改变 selected；同步会话的目标不能跟着 selected 偷换。必须冻结独立的会话绑定目标，并在焦点变化时暂停。
- 现有源码有异步延迟和串行队列：串行队列不代表跨 asyncAfter 的整个输入事务不可交错；需要显式的会话仲裁。

## 3. 技术路线选择

| 路线 | 能力与限制 | 决策 |
|---|---|---|
| AX 辅助功能读写特定输入框 | 能读回、检测冲突，部分控件可设置 AXValue；应用支持程度不同 | 首版，先做兼容探针，再开放通过验收的控件 |
| CGEvent 逐字追加/退格修订 | 追加容易，修订受光标、选区、Unicode、IME、焦点影响 | 不作为静默降级路径；保持旧式一次性发送 |
| 反复 Cmd+A + 粘贴 | 可能全选整个文档，改剪贴板、打断撤销与候选态 | 禁止作为通用实时方案 |
| Mac 原生输入法 InputMethodKit | 有输入会话/marked text 机制，但需安装启用输入法，改变部署与使用方式 | 若核心目标控件 AX 不可用，再独立评估；不并入首版 |
| 浏览器扩展/应用插件 | 可直接参与特定编辑器状态，但有分发与逐应用适配成本 | 仅在明确高频目标验证失败后评估 |

Apple 提供属性可写性查询和属性设置 API，但不保证每个应用支持。[2][3] 本机 SDK 的 AXAttributeConstants.h 明确把 AXSelectedText 描述为只读，AXSelectedTextRange 描述为可写；不能把「设置选中文字」当通用替换方案。首版使用经能力探测的 AXValue 整框设置，且限定空白、独立的纯文本草稿框。

整框设置也可能破坏富文本/撤销、仅改变显示却未改变框架模型，必须验证「编辑后失焦仍保留」「提交后真实正文一致」。AX 读回一致只代表控件值被观察到，不能证明消息已发送。原生输入法 setMarkedText 则是另一层协议，不是后台 helper 可任意调用的跨进程写入接口。[4]

## 4. 首版范围与用户流程

### 4.1 范围

- 支持：明确开启、纯文本、绑定单一空白 Mac 输入框，手机原有草稿可作为首次快照；新增、删除、撤销、本地中间编辑都同步。
- 不支持：富文本正文、代码编辑缓冲区、终端、密码框、搜索/命令框、UU/其他无法读回的远控输入、附件、桌面和手机同时编辑、自动寻找新输入框。
- 不把整个应用标记为「所有输入框都支持」；兼容性按应用版本、控件/页面和写入方式记录。AX role 只是必要条件，不是安全性证明。
- 同步最大 8,000 UTF-16 单元，沿用当前文本上限；JSON 请求体另设适当字节上限，不能把字符上限误当 UTF-8 字节上限。
- 原有一次性发送保持可用，但同步途中发生错误时不能自动切旧路径重发全文。

### 4.2 流程

1. 用户选应用，点击「实时同步」。服务端只在 begin 时允许复用激活能力；目标输入框需由用户在 Mac 或手机画面中点选。
2. begin 检查具体 pid/应用身份、窗口、当前焦点 AX 元素、role/subrole、可读写性、空字符串值、零长度选区，以及已经通过验证的控件支持策略。密码框等在读取内容之前按角色拒绝。
3. 框内已有内容（空格/换行也算）则不清空、不合并，提示「电脑输入框已有内容，请换一个空白输入框」。第一版不拉取电脑旧草稿到手机。
4. 创建 session，返回应用名称；手机显示「正在同步到 X」，首次发送当前完整草稿。进入同步本身不清空手机内容。
5. 输入时显示「同步中」；指定 revision 读回核验完成后显示「已同步」。用户继续输入时立即恢复「同步中」，不能让旧确认遮盖新修改。
6. 「停止同步」只停止写入，保留两端内容，不等于撤销或清空。停止后同一份草稿不得无提示经旧 send() 再追加；明确标记它已在电脑，需用户主动处理或开启新草稿。
7. 手机把文本删除为空是明确的草稿编辑，可把本会话绑定框清空；仅在会话健康且值仍与上次确认一致时执行。
8. 切应用、点触控板/画面、切换 Dock 目标、触发快捷键、页面隐藏、断线、AX 元素失效都暂停会话；保留手机草稿和最后确认信息。
9. 恢复需显式操作，重新校验同一元素和值。不得按旧元素路径自动绑定一个新框，不得静默追随新的前台应用。

输入中手动点 Mac 其他位置同样暂停。停止、冲突后的第一版可不提供自动恢复，提示确认电脑残留文字后重新开始；不要为了「重连无感」增加误覆盖风险。

## 5. 协议：先用已有 HTTP，传完整快照

采用现有同源 HTTP 与 authHeaders()，不必为了文字流式感改 WebSocket。约 100–150ms 合并一次文字变化只是初始实验值；最多一个更新在途，另存一个最新快照。每次都传全文，服务端直接比较预期值；8,000 单元范围内先不引入 OT/CRDT 或增量补丁。

请求通过项目现有鉴权边界；状态查询也需鉴权，不能把草稿放进公共 /api/status。会话 ID 随机生成，绑定客户端随机 ownerId 和已认证身份；ownerId 不是替代认证的秘密。首版全局仅一个实时输入会话，其他客户端收到 busy，不可抢占。

建议端点（字段命名可遵循仓库现有约定，语义不可省略）：

| 端点 | 请求 | 结果与约束 |
|---|---|---|
| POST /api/live-input/begin | requestId, ownerId, targetId | sessionId, resolvedTarget, appliedRevision=0；同 requestId 重试不能创建第二个会话 |
| POST /api/live-input/update | sessionId, ownerId, revision, text | appliedRevision, outcome, state；revision 单调递增，允许跳号 |
| POST /api/live-input/status | sessionId, ownerId | 当前 state/appliedRevision/提交状态，不回传输入内容 |
| POST /api/live-input/end | sessionId, ownerId, requestId | 结束并保留目标草稿，重复请求幂等 |
| POST /api/live-input/commit | sessionId, ownerId, commitId, finalRevision | 仅第二阶段，见下节；绝不携全文重新输入 |

具体规则：

- revision 由客户端分配；同 revision 同 text 是重试，返回原确认；同 revision 不同 text 拒绝。旧 revision 不得覆盖较新值。
- 服务端按会话串行处理。重复 revision 返回缓存前也检查会话是否已冲突/结束；不得误报仍健康。
- 待发快照可被更新版本替代，在途请求不可当成没有发生。HTTP 超时或 AbortController 取消不等于 Mac 没写入。
- 请求超时先进入 uncertain 并查询 status，不能立即走旧发送路径。查询未知会话或服务器重启后，保持草稿、停止自动写入，提示检查电脑残留。
- 只有读回一致才更新 appliedRevision。API 返回成功但核验失败时是 uncertain/failed，不能把待发版本推进为确认版本。
- 服务端持有输入独占权：live session 期间 /api/send 拒绝 busy；PointerExecutor/快捷键/应用激活的互斥要有共同仲裁入口。控制动作先令会话失效，且让已在执行的 AX 写入结束，再执行动作；不能各自队列抢跑。
- 每次异步边界重新检查会话 generation/关闭状态，end 后的排队更新无效。以服务器执行顺序定义边界，不承诺能撤回已执行写入。
- AX 调用使用有界超时，在专用执行队列运行，不能卡住网络接收、截图和光标广播。
- 会话仅内存保存；以显式心跳维持短租约（初始建议心跳 2 秒、过期 10 秒，实测调整）。页面隐藏尽力 end，不能依赖 unload 一定送达；租约过期只停写、不清内容。
- 日志只记录 session/revision/字数/耗时/结果，不记录正文、token 或用户原始选区内容。过期释放 AX 引用与内存草稿。

## 6. Mac 写入算法和并发边界

首版只对已经通过探针的空白独立纯文本框启用 AXValue 镜像；不要默认支持所有 AXTextArea。

每次 update：

1. 验证认证、会话所有者、租约、revision、长度和仲裁权。
2. 验证原应用实例、原窗口、原 AX 元素仍有效，当前输入焦点确为该元素；不要只看 Util 的视觉前台结果。窗口可见性和键盘焦点是两个判据，不改写已有全局前台探测规则。
3. 读值与选区；与上次确认的值及程序预期光标相同才继续。不同则 conflict，立即停止；即使是应用自动改写也不能强行覆盖。
4. 对同样 text 不重复设置。对新 text 使用可写 AXValue 设置整框值，不动剪贴板，不调用 Return，不发送退格，不拼接全文。
5. 根据探针验证过的控件行为，把 Mac 插入点保持在文末（需要 AXSelectedTextRange 可写且经实测）。手机可以在中间编辑，但首版不镜像手机选区。
6. 有界重读文字、焦点、选区，完全匹配后确认 revision；只读重试，不盲目重复写入。应用若自动归一换行/Unicode，要单独验证明确转换规则，不能用 trim/模糊匹配掩盖差异。
7. 发回确认后若有更新快照，继续处理最新一份。

Swift String.count、JS length、AX range 不能混算：协议长度定义 UTF-16；NSRange/CFRange 转换和具体控件索引语义要用 emoji、组合字符、ZWJ 家庭表情实测。首版整框写避免了「退格 N 次 = 删除 N 个字符」的错误假设。

重要限制：AX 跨进程「读—写—读」不是原子比较交换。检查与写入之间仍可能发生实体键盘编辑，读回也不能证明绝对没有竞态。使用焦点/值变化通知加每次读前检查能缩小窗口，但不能消除；产品明确会话期间由手机编辑，用户碰电脑就暂停。若验收要求并发编辑零丢字，必须转向应用插件/编辑器事务接口或原生输入会话，不应宣称普通 AX 能保证。

## 7. 手机输入与语音修订

- 以 textarea.value 完整快照为真值，监听 input；不要用 keydown 拼文本，粘贴、听写、自动纠错都不一定对应按键。
- 使用 compositionstart/compositionend 与 InputEvent.isComposing 维护组合态；组合期间首版不向 Mac 写未确认候选，在 compositionend 后读取完整 value，并与随后 input 去重。[5]
- 监听程序性文本变更：历史回填、清空、恢复草稿也必须走同一更新入口；JS 修改 value 不会自动等价于真实 input。
- 不使用 trim，保留首尾空格、换行；composition 期间禁止提交，避免候选确认被解释为发送。
- 若手机输入法直接产生非 composing 的逐步修订，完整快照会自然替换旧文字，不把两次识别结果追加在一起。
- 若实机只在说完才交付文字，明确报告观察结果。不要加轮询、模拟 keypress 或 CSS 动画假装获得实时识别。
- Web Speech API interimResults 是另一条识别路线，兼容性受限；本任务不引入。以后若要跨输入法保证音频级流式结果，应单独规划自有 ASR，包含录音权限、HTTPS、费用与数据处理。[6]

## 8. 提交：与同步分离，第二阶段交付

阶段一可交付「同步 + 停止，Mac 手动提交」的实验；完整用户功能需阶段二完成提交。不要在实验模式保留会把全文重复追加的旧发送按钮。

进入阶段二后，手机按钮文案「发送到 X」，流程：

1. 等待组合态结束，冻结提交快照；提交期间短暂禁用编辑和再次提交，避免新输入被成功回调清空。
2. 立即 flush 最终快照，等待 finalRevision 真正确认。
3. commitId 唯一；服务端确认 finalRevision 是最新版本，原控件仍有该内容与焦点，再执行一次该控件经过验证的提交动作。普通编辑器不支持消息发送则仅提供「完成同步」，不要把 Return 当通用提交。
4. 复用现有按键注入底层能力，不调用 performPaste()/send()。提交控件若需 Cmd+Enter 等组合，由明确应用适配决定，不能猜。
5. commit 必须在服务端登记后执行，重复 commitId 不重发；回执丢失查询状态，不自动再次按回车。进程崩溃时无法保证外部按键 exactly-once，重启后返回未知并让用户核对，不能恢复并重放。
6. 「已同步」是文字核验；「提交动作已发出」是键盘事件执行；「消息已发送」需要应用级证据。输入框变空也不能单独证明服务端已接收消息。
7. 提交结果不确定时保留手机草稿，冻结自动发送；明确提醒核对 Mac。不要点一次「重试」就把全文再写一遍。
8. 已确认本地提交动作完成后关闭会话并归档草稿（可标记远端发送未确认），清空只针对被冻结的这份草稿。下一条消息必须新建会话，重新绑定空白输入框。

附件首版禁用实时模式下的选择入口，不自动沿用全局 pendingImage。扩展图文提交需另行设计按 session/commit 归属的附件暂存和消费语义。

## 9. 代码组织与执行顺序

建议最小新边界，执行者可按仓库现状合并纯类型，但不把状态机堆进已有大文件：

- Sources/LiveInputSession.swift：会话、版本、租约、冲突、幂等和输入仲裁；不包含 HTTP 解析。
- Sources/EditableTextTarget.swift：具体 AX 元素绑定、能力读取、写入与核验；不持有手机 UI 状态。
- Web/live-input.js：输入事件、组合态、快照节流、请求与状态展示；复用 authHeaders 和现有消息/历史入口，通过明确接口与 app.js 协作。
- Server.swift 只增加路由转发；main.swift 组装；需要抽取 InputExecutor 的公共底层能力时保持旧发送行为，不复制按键实现。
- 必要时独立纯协议类型文件；每个新业务文件都有 L3，新增成员更新 Sources/Web 的 L2、根 L1 和 README；资源增加后更新脚本加载与统一 ?v= 版本号。

实施里程碑：

**P0 兼容性验证（先于正式开发）**：制作仅在用户明确选定的测试框运行的小型 AX 探针，输出能力与结果，不输出正文。测试原生纯文本框、用户实际使用的聊天输入框、Chromium/Electron 控件；记录应用/系统版本、role、value 可读写、选区、写后读回、失焦保留、原生手输续接、撤销、最终真实提交。测试消息只在明确授权的测试会话中提交，不向真实联系人发送。至少一个用户核心目标通过，否则交付失败证据及后续路线，不交付假通用支持。

**P1 草稿闭环**：实现 begin/update/end/status、单所有者、版本、读回、暂停和手机反馈。先用普通打字跑通，再测听写。支持范围基于 P0 证据，不根据应用名字猜。

**P2 提交闭环**：finalRevision 屏障、commit 幂等、历史/草稿保护、针对测试应用的提交适配，完成原发送、快捷键、触控板的仲裁回归。

**P3 真机验收与交接**：执行下面矩阵，记录通过/失败/未测；公布支持范围和剩余风险。没有手机实机条件不能写「iPhone/Android 已验证」。

本任务无需安装新依赖、搭语音服务、改 HTTPS 或重做主题。普通文本事件本身不依赖陀螺仪权限；如果后续引入网页录音再处理相应安全上下文。

## 10. 验收矩阵与成功标准

| 场景 | 必须结果 |
|---|---|
| 连续输入、整段粘贴、停顿 | Mac 当前框等于最新确认快照；无重复段落、无自动 Return |
| 语音从「明天三点」改成「明天下午三点」 | 替换旧值，不追加两份句子 |
| 拼音选词、日文组合、手机听写 | 候选确认不发送；如只能整句交付，如实标记 |
| 中间插字、删除、全清、手机撤销 | 最终两端一致，空白字符不丢 |
| emoji、组合重音、ZWJ、换行、8,000 单元边界 | 文本不截断、不拆代理对、超限不部分覆盖 |
| Mac 目标已有草稿 | begin 拒绝，原文完整 |
| Mac 用户输入、移动光标、切窗口/应用 | 暂停且不重新抢焦点；手机草稿保留 |
| 网页重渲染、窗口关闭、应用重启、权限撤销 | 旧会话失效，不能写到新元素 |
| 延迟、乱序、重复、更新确认丢失 | revision 不回退、不重复覆盖；不确定状态可见 |
| 断网/重连、页面后台、Mac 服务重启 | 不回放旧命令，不自动清空任何一端 |
| 两个手机同时开启 | 第二个 busy，不打断第一会话 |
| 点提交时尚有待同步文字 | 等最终 revision 确认后只提交一次 |
| 提交超时或应用无明确发送证据 | 保留草稿、标记未确认，不自动重试 Return |
| 同步期间触控板、快捷键、旧 /api/send | 按仲裁规则先停止或拒绝，无写入交错 |
| 关闭实时功能 | 原一次性文字/图片发送、Dock、截图、光标通路正常 |

单元测试集中在状态机/幂等/超时/版本竞态与 Unicode 边界；假 AX target 可注入失焦、内容变化、写入超时和元素失效。真实应用测试负责证明 AX 修改进入实际编辑器模型，两者不能互相替代。

延迟实验目标：局域网健康连接、支持控件下，从网页 input 到 Mac 核验回执 P95 ≤300ms（目标，不是已测结果）；单独记录 AX 写入和网络 RTT。看手机截图仍可能约 1fps，不把截图刷新延迟误归因文字输入，也不把本地拟合预览当真实 Mac 内容。

核心通过门槛：一轮连续编辑至少 100 次变更最终完全一致；20 次正常提交无重复；断线/切目标等负面用例不向错误输入框写入。报告测试样本与限制，不从有限次数推断绝对无竞态。

## 11. 可直接交给执行模型的指令

请在当前 PocketDesk 仓库按 LIVE_INPUT_PROPOSAL.md 实现手机输入框到 Mac 指定输入框的实时草稿同步。先读最新 GEB 文档与真实代码，完成 P0 能力验证后再开放通过验证的控件；按照 P1、P2、P3 完成会话绑定、完整快照同步、读回核验、独立提交和真机验收。不要循环调用现有 /api/send，不要每次写入后按 Return，不要反复 Cmd+A/退格覆盖，不要在断线后重放全文，不要宣称全应用兼容。不新增语音识别服务，不实现陀螺仪，不改主题。所有修改同步 L3/L2/L1，保留其他任务未提交改动。若实际核心应用不支持 AXValue 镜像，提供实测失败证据和最小替代路线，不用不可靠输入注入冒充成功。最终交付代码、支持矩阵、测试记录及残余风险；未实测项目必须标明。

## 12. 资料与证据

1. [豆包输入法官网](https://srf.doubao.com/pc)：核实其公开产品能力；没有据此推断私有技术实现。
2. [Apple AXUIElementSetAttributeValue](https://developer.apple.com/documentation/applicationservices/1460434-axuielementsetattributevalue)：辅助功能属性设置接口。并核对本机 MacOSX.sdk 的 HIServices/AXUIElement.h：属性不支持、对象失效、消息失败均为合法错误结果。
3. [Apple AXValue 属性](https://developer.apple.com/documentation/applicationservices/kaxvalueattribute)：控件值语义。本机 HIServices/AXUIElement.h 核对 AXUIElementIsAttributeSettable；AXAttributeConstants.h 核对 SelectedText/SelectedTextRange 的只读/可写声明。仍需逐控件运行时探测。
4. [Apple InputMethodKit 控制器](https://developer.apple.com/documentation/inputmethodkit/imkinputcontroller)、[NSTextInputClient.setMarkedText](https://developer.apple.com/documentation/appkit/nstextinputclient/setmarkedtext(_:selectedrange:replacementrange:))：原生输入会话/组合文本路线，不能等同 CGEvent 注入。
5. [MDN InputEvent.isComposing](https://developer.mozilla.org/en-US/docs/Web/API/InputEvent/isComposing)：区分组合输入期间事件；手机实际事件序列仍需测试。
6. [MDN SpeechRecognition.interimResults](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/interimResults)：中间识别结果及兼容性限制；不等同系统输入法向 textarea 暴露结果。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
