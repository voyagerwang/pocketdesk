# PocketDesk × Agent × Workbench：整体产品与架构方案

> 版本：v0.1，2026-09-09。设计交接稿，不代表已实现。
> 设计责任：本方案负责产品边界、架构契约、实施次序及验收；执行模型按阶段实现，不自行扩展成另一个通用 Agent 平台。
> Workbench 的真实仓库/产品接口尚待用户提供。本文定义它应承担的集成角色，不声称现有 Workbench 已支持这些接口；实际接入必须经过 M0 核对。

## 1. 产品决策：一开始就一起设计，分阶段实现

PocketDesk 的目标是「随身办公指挥台」：用手机交代工作，在 Mac 上执行，手机收到可信结果，必要时人工接管。输入、遥控、Agent、记忆和 Workbench 是同一工作过程的不同环节。

第一天必须有：统一任务 ID、执行者身份、任务状态、证据分级、记忆作用域、路由规则、执行权限、设备控制权、成本记录、Workbench 适配边界。

不要求第一天实现：多 Agent 并行、向量数据库、持续录音、全自动电脑操作、自学习改提示词、跨设备调度集群、所有办公软件适配。整体设计是避免后续推翻契约，不是一次性实现全部能力。

### 体验承诺

- 用户说任务，不必记住一串按键；明确的普通操作不调用模型也能完成。
- 用户知道任务交给了谁、是否真的开始、结果在哪里。
- 「以后这样做」可以成为可查看和撤销的规则；规则不会隐性扩大执行权限。
- 手机、Workbench 指向同一个任务，可以补充、查看、接管和继续。
- 复杂程度、延迟和 token 成本可控，不以无限重试换取表面成功。

## 2. 当前项目能力与证据边界

当前仓库已存在应用目标配置、激活、键盘/指针注入、手机画面、配对鉴权、快捷键和最近动作日志；Server 还出现了 /api/live-input，说明其他模型正在实现相关能力，执行前必须重读源码。

ExecutionLog 是容量 40 的进程内环形记录，不是持久任务系统。ExecutionOutcome 的 delivered/sent/blocked/failed 可以复用为动作证据词汇，但不能把它直接当 Agent 长任务状态。

README 说明 PocketDesk 不占用或伪装 Workbench 的 Bonjour 服务。本设计保持独立应用/服务身份，通过显式适配连接，不猜测端口、不共用数据库文件、不扫描凭证。

本次仓库检索未取得 Workbench 的实现。已查到 WorkBuddy 官方连接器文档支持 MCP + Skill、CLI + Skill；这证明可以扩展其工具，不证明用户当前桌面版本存在可调用的外部派单/取消/结果 API。两种方向必须分别验证。[S1]

## 3. 角色与数据流

```text
手机 PocketDesk                   Workbench
说话 / 输入 / 看结果 / 接管        项目 / 任务 / 资料 / 规则视图
           \                      /
             Task Service（唯一任务事实源）
                │
     Intent → Policy + Rules → Router
                │
          Executor Adapter
       ┌────────┼──────────────┐
       本地动作   Agent 任务接口  GUI 派单/Computer Use
       └────────┼──────────────┘
          证据 / 结果 / 成本 → Task Service
```

逻辑上 Task Service 是唯一任务事实源。M0 核查 Workbench 是否已有合适任务服务：有则复用其事实源、PocketDesk 做客户端；没有则第一版由 Mac helper 承载 Task Service，Workbench 经 API 访问。不能两个服务各自写一套权威任务状态再靠模型猜测同步。

手机保持轻量，不保存云模型 API Key，不承担持续运行调度。Mac 负责工具调用、绑定资源、执行仲裁和凭证访问。记忆由确定的 Memory Service 管理，模型只提出可校验的读取/写入请求。

模型是理解与规划部件，不是数据库、权限裁判或成功判定的唯一来源。优先简单固定工作流，再为开放任务引入自主规划，符合成熟 Agent 设计对工作流与 Agent 的区分。[S2]

## 4. 用户入口与办公场景

### 4.1 输入与办事必须显式区分

现有「输入」原样把文字送到目标应用；新增「办事」理解并执行要求。两者可共用输入框，但按钮和当前模式必须清楚，不能把用户准备输入给别人的“删除文件”直接执行。

首版沿用手机键盘的语音输入，用户结束说话/点击执行后才提交。识别过程只更新草稿，不执行半句话。独立 ASR 和常听唤醒不属于首版，识别延迟/成本与 Agent token 成本分别统计。

### 4.2 六个首要工作流

| 用户表达 | 路由与执行 | 最小可信反馈 |
|---|---|---|
| 打开浏览器 | 本地应用匹配与激活 | 已确认打开，或已发出但无法确认 |
| 浏览器搜索某个主题 | 编码搜索词，按配置搜索引擎打开 URL | 搜索页面已打开；不默认总结 |
| 让 WorkBuddy 整理这份资料 | 解析指定资料、派给指定 Agent | 已投递/已接收有证据区分；结果有来源 |
| 把当前网页总结给我 | 捕获绑定的 URL/正文，交总结任务 | 摘要 + 原网页链接 |
| 帮我安排明天下午三点的会议 | 相关路由规则选择执行者，补齐必要参数，核对时区与参与人 | 日程草稿或真实事件 ID/链接 |
| 继续刚才的任务，把第二点展开 | 使用显式当前 taskId 与结果引用继续 | 同一任务的新修订，不创建无关任务 |

“这个网页/文件/它”通过当前任务或用户选择绑定；存在多个候选时问一个必要问题。浏览器搜索和阅读总结是不同任务，不能自动追加昂贵步骤。URL 构造允许名单与编码校验，不把自然语言拼接成 shell 命令。

“明天”等相对时间按请求时的用户时区解析，并保存绝对时间、时区及原文。邀请人身份必须解析为确定对象；缺少结束时间等参数可使用用户已明确保存的默认值，否则询问。

### 4.3 人工接管是产品能力

任务卡常用动作：查看画面、补充要求、暂停/停止、接管、继续。用户手动接管先撤销 Agent 的 GUI 控制权，确认释放按键和暂停后再接收手机鼠标；回交后重新观察环境，不沿用接管前截图继续点。

暂停 GUI 不等于取消 Agent 的后台计算。若某适配器无法暂停/取消，明确标注「无法停止远端任务」，不能用停止轮询冒充停止执行。

## 5. 路由：偏好与机制分开

三个执行层：

1. 明确本地动作：确定性规则匹配已知应用/动作，零 LLM 推理调用。
2. 固定流程：一个小型意图解析调用生成结构化参数，代码执行并验证。
3. 开放任务：交给有相应能力的 Agent。用户指定执行者时不再让另一个 Agent 重复规划全文。

路由优先级：本次明确要求 > 项目规则 > 用户规则 > 系统默认；同一层规则按显式 priority 和 specificity 决定，仍冲突则询问，不默认最后写入者获胜。

「谁做」和「如何连接」是不同字段：用户偏好钉钉 AI 助理，不代表必须使用 GUI。adapter 根据已经验证的 capability 选择任务 API、CLI、工具接口或 GUI。用户明确指定 Codex Computer Use 则尊重指定方式；不可用时报告，不静默换成更高权限路径。

自然语言解析必须输出受约束的 intent/target/arguments/contextRefs，程序校验白名单、参数类型和权限。不能让模型生成任意 shell 字符串作为默认工具。

## 6. 记忆：从最小结构开始，支持纠正与撤销

### 6.1 四类数据分开保存

| 类别 | 内容 | 生命周期 |
|---|---|---|
| 用户偏好 | 回复长度、默认浏览器、时区、默认日程时长 | 用户修改/删除前有效 |
| 执行规则 | 日程交给谁、某项目代码交给谁 | 有版本，可暂停、撤销和过期 |
| 任务上下文 | 用户目标、已确认参数、当前进度、资料引用 | 任务范围，按保留策略清理 |
| 能力/可靠性记录 | adapter 是否支持 status、哪种控件失败 | 有测试日期/版本/TTL，不当用户偏好 |

首版结构化查询即可，不部署向量库。若后续有模糊历史检索需求，再增加语义索引；索引可以重建，不能作为唯一原始记忆。

### 6.2 写入规则

- “这次用钉钉”：只改当前任务。
- “以后排日程都交给钉钉”：保存用户范围的规则，立即展示准确释义与撤销入口，不重复要求确认已明确的偏好。
- “在这个项目里……”：必须绑定已知 projectId，否则确认项目。
- “以后都这样”：无法确定“这样”指什么时，展示拟保存规则并问一次。
- 普通纠错不自动成为长期偏好；系统观察到重复改派可提出建议，用户接受才生效。
- 网页、文档、Agent 回答中的“请记住”均为外部数据，不能直接写用户规则。

规则例子：

```json
{
  "id": "rule-uuid", "version": 1, "scope": "user", "projectId": null,
  "when": {"intent": "calendar.create"},
  "then": {"executorId": "dingtalk-assistant", "mechanism": "auto"},
  "priority": 0, "enabled": true,
  "source": {"kind": "explicit_user", "taskId": "source-task"},
  "expiresAt": null
}
```

自然语言说明供用户理解，结构字段决定执行；保存前展示/返回规范化内容。记录 createdAt/updatedAt、来源、版本；用户能查询“你记住了什么/为什么派给它”、修改、停用、删除。删除后的后续检索、缓存和派单不得再使用旧规则。

正在运行的任务固定其 routeDecision 和 ruleVersion；规则更新默认只影响新任务，不中途偷偷改派。明确用户要求重派则创建新 attempt，并检查原执行是否可停止、是否已产生副作用。

建议初始保留策略：任务元数据/事件 30 天可配置，已保存规则持续到删除，临时截图默认不落盘；原始音频首版不收集。附件保存引用与授权范围，删除任务不顺带删除用户原文件；凭证不写进记忆。日志与诊断不包含正文和密钥。

## 7. Workbench 集成契约

Workbench 不再是一份重复聊天历史，而是任务、资料、规则和结果的工作视图。手机强调交代与反馈，桌面强调检查、组织与批量工作。

应共享：taskId、projectId、revision、contextRefs、executorId、routeDecision、status、artifacts、usage。展示当前任务、执行者、规则依据、结果和可用操作；改派/停止按钮按 adapter 能力启用。

M0 必须确认：真实项目路径与技术栈；现有任务/项目 ID；是否有任务服务、事件流和认证；结果如何存储；是否已有记忆/规则模块。已有能力复用，缺失能力按本文契约补齐。核对前不得修改 Workbench 仓库或假定某端口/协议。

接入层使用 WorkbenchBridge：读取项目与上下文引用、呈现任务、接收操作、打开结果。跨设备时用受认证 URL/下载端点，不能把 Mac 的绝对文件路径当作手机可下载链接。

如果 Workbench 已有权威任务服务，手机命令最终写入它；Mac 保存必要执行恢复信息及映射。如果没有，则 helper 的 TaskStore 为权威，Workbench 通过 API 投影。事实源选择在 M0 决定，后续迁移必须有 ID 映射与版本策略，不支持两边离线同时随意写。

## 8. 任务模型、状态与可靠反馈

最小 Task：id、revision、projectId、source、rawRequest、normalizedIntent、contextRefs、routeDecision、status、attempts、artifacts、usage、createdAt/updatedAt。原文只在授权任务存储中保存，不写一般日志。

TaskAttempt：attemptId、executorId、externalTaskId、capabilities、status、startedAt/finishedAt、evidence、error。一个任务可有多次尝试，但不可把多次副作用伪装为一次成功。

推荐状态：

```text
received → needs_input / ready → dispatching → submitted → running → succeeded
                                   │             │          │
                                   └─ uncertain ─┴──────────┤
                                                          failed
running → pause_requested → paused → running
running → cancel_requested → cancelled / uncertain
```

仅在适配器提供对应证据时进入 running/paused/cancelled。submitted 可长期保留在 GUI 派单场景，并显示「已投递，无法跟踪后续」；不得用计时结束或输入框清空推断任务完成。

每条事件含 eventId、taskId、seq、type、timestamp、payloadVersion。客户端以服务端 seq 去重排序；断线按游标补取，遇保留期缺口获取任务快照重新建立游标。

结果证据分层：请求被接受；已投递；执行者确认接收；执行者报告完成；产物存在且通过约定验证。业务结果按验收定义，例如日程需要事件 ID/时间/参与人，而不是“按钮点过”。AI 产物报告来源和未核对部分，不一律冒充独立事实核验。

普通动作模板回复即可；复杂任务完成后最多一段摘要加产物入口。进度只在阶段变化、完成、失败或需要用户时推送，默认不每几秒播报一次。

## 9. Adapter：先有能力声明，再谈自动化

每个执行适配器声明：submit、status、events、cancel、resume、artifacts、requiresGUI、supportsIdempotency，以及当前测试版本/日期。

- NativeActionAdapter：复用应用激活等已有能力，增加浏览器 search/open URL 等语义动作。
- AgentTaskAdapter：只对已验证的真实任务 API/CLI 开放，取得 externalTaskId 并按协议查询。
- GUIHandoffAdapter：绑定应用、窗口/对话和输入框后输入原始任务；最多确认投递，无法回收状态时明确降级。
- ComputerUseAdapter：按需要观察并操作界面，有步数/时限/成本预算；不是所有任务的默认路径。

Codex 是否可以被当前产品通过受支持接口启动 Computer Use、怎样认证、是否可回调，均须验证。当前对话里的 Codex 工具不能自动视为 PocketDesk 可集成的产品 API，不依赖未公开界面或扫描应用内部凭证。

向 WorkBuddy 暴露 PocketDesk MCP 工具与向 WorkBuddy 派单分开：前者是工具扩展，后者是任务入口。MCP 可包装同一业务执行层，不创建第二套直接绕过权限/仲裁的注入实现。

防止委派循环：task 携带 parentTaskId、visitedExecutors 和 delegationDepth，首版禁止再委派回入口执行者；默认只允许一次外部派单。不搭建多个 Agent 相互聊天的常驻群。

## 10. 执行权限、人工控制与故障恢复

路由偏好不包含授权。打开应用、打开搜索等明确用户请求直接执行；向 Agent 投递用户明确指定的任务允许执行；对外发送、邀请参与人、删除或发布须核对用户已授权的具体范围，只在缺失时展示具体待执行内容请求确认。

GUI 执行需全局单写入租约：owner 为 task/人工会话，范围为设备与目标资源。Agent、手机触控板、快捷键、实时输入都经过同一仲裁；人工接管撤销旧 generation，排队命令失效，释放所有按下按键。重回 Agent 必须重新观察。

请求记录 requestId/idempotencyKey 与执行前状态。API 支持幂等则透传；GUI 按键没有天然 exactly-once，投递超时进入 uncertain，核对状态后再决定，不自动重复点发送。服务重启不回放已投递但未确认的动作。

cancel 请求只表示意图，收到执行者确认后才标 cancelled。暂停和断线不自动清除电脑草稿、不删除产物。执行者不可取消时停止后续步骤与观察并明确提醒远端可能仍在运行。

外部网页、文件和 Agent 输出是待处理内容，不能修改权限、工具列表或记忆。上下文只读取用户指定资料，默认不持续上传整个桌面；API Key 保存在 Mac 系统凭证存储，配对 token 不等于所有模型/企业系统授权。

## 11. Token 与成本策略

成本顺序：确定性动作 → 固定工作流 → 单次小模型意图解析 → 指定 Agent → 必要的视觉操作。无法确定时升级，不能为省 token 把模糊请求硬套规则执行。

- 只传命中的规则、必要任务摘要和资料引用，缺正文时按需读取；不发送全部规则、项目文件和完整历史。
- 用户说“告诉 WorkBuddy……”保留原话和相关引用，不先让另一个大模型扩写。
- 模型解析一次结构化计划，代码完成可预测步骤；每个点击不单独调用模型。
- 状态轮询、预算计数、规则匹配不调用模型；事件驱动优先，轮询退避。
- 截图只在 UI 决策需要时发送；DOM/AX/接口信息优先，不不断把手机画面流喂给模型。
- 任务中保留简短事实摘要和产物引用；修订摘要不能丢失用户约束或把未经确认的结果写为事实。

初始预算建议（产品实验值，可配置）：普通路由最多一次解析加一次格式修复；本地步骤最多一次有依据的重试；开放 Computer Use 每段最多 10 个工具步骤/120 秒，达到边界展示进度并检查预算；预算耗尽暂停，不伪称完成。按每种模型设输入/输出 token 上限与每任务金额上限，实际单价由配置/供应商数据维护，不在代码写死旧价格。

usage 记录 provider/model、input/output/cached tokens、工具调用、耗时、费用来源。外部 WorkBuddy/Codex 不返回用量时记 unknown，不记 0；语音识别、模型、搜索等费用分别列出。

验收优化看每个成功任务的成本、成功率、时间、重试与人工接管率，不单看单次 token。首版不承诺“节省 X%”，用同一任务集跑基线再报告。

## 12. 最小接口和持久化

以下是新逻辑接口建议，不是现有接口声明。统一版本前缀如 /api/v1；若复用 Workbench 服务则映射到其现有契约。

| 接口 | 语义 |
|---|---|
| POST /tasks | 创建任务，requestId 去重；返回 taskId/revision，不等待长任务完成 |
| GET /tasks/:id | 状态、证据、能力和产物引用 |
| GET /tasks/:id/events?after=seq | 可恢复事件读取，初版短轮询即可，后续 SSE |
| POST /tasks/:id/actions | supplement/pause/cancel/takeover/resume；带 expectedRevision、requestId |
| GET /rules | 按作用域查询生效规则与来源 |
| POST/PATCH/DELETE /rules | 创建/修改/删除规则，写入带版本校验 |
| GET /executors | 当前能力、可用性与已验证集成信息 |

所有新读写端点鉴权、限制对象访问；任务正文不能混进公共 /api/status。并发修改不满足 expectedRevision 返回冲突，客户端刷新再操作。控制回执/任务事件不能使用现有 WS 光标的 latest-only 单槽队列。

若 helper 承担 Task Service，建议 SQLite 原生绑定保存 tasks/attempts/events/rules/usage，事务确保状态和事件一致，包含 schemaVersion/迁移测试；先验证本机 Swift 编译与打包链接方式，不引入服务端框架。若 Workbench 已有存储则复用其迁移机制，避免另建同职责数据库。凭证与附件原文件不入这些表。

## 13. 模块边界与现有方案关系

建议逻辑模块：TaskService/TaskStore、IntentRouter、RuleStore、ExecutorRegistry/Adapter、ExecutionCoordinator、WorkbenchBridge、UsageLedger。按阶段落文件，不为尚未实现的 adapter 建空框架；现有 Server 只做路由，InputExecutor/PointerExecutor 只做受控执行。

手机 UI：办事入口、任务卡/详情、人工接管、简洁规则入口。Workbench：项目任务列表、规则管理、结果与执行依据；产品设置和高级调试尽量留桌面。现有 console 的配对/权限流程保持可用。

LIVE_INPUT_PROPOSAL.md 定义输入草稿同步，不承担任务路由；FULLSCREEN_POINTER_PROPOSAL.md 定义手动操控，需接同一个控制租约；CURSOR_VIEW_PROPOSAL.md 定义画面和观测真实性；陀螺仪是可选输入手势，不负责推断任务或绕过控制权。

源文件增改遵循 L3/L2/L1 回环；新模块补地图。设计稿中的目录不是现有文件清单，未实现前不把架构写成已经上线。

## 14. 实施阶段与交付门槛

| 阶段 | 工作 | 验收与退出条件 |
|---|---|---|
| M0 集成核对 | Workbench 事实源、WorkBuddy/Codex 入口与回执能力、现有代码变更 | 提交 capability matrix 和 FACTS.md；每项标已验证/未验证/不支持，确认事实源归属 |
| M1 任务底座 | 任务 ID/状态/事件/持久化/权限/租约/usage，手机任务卡，本地打开/搜索 | 重启不重放副作用；反馈与实际状态一致；常规动作无模型调用 |
| M2 规则与首个 Agent | 作用域规则、明确记忆与撤销、一个真实派单 adapter | “以后交给谁”下一次生效；本次覆盖不改长期规则；GUI 无结果回执时如实降级 |
| M3 Workbench 闭环 | 同一任务视图、上下文绑定、补充/接管、真实结果返回 | 手机发起、桌面查看、手机续接一致；至少一个 Agent 有可靠结果闭环 |
| M4 开放自动化与优化 | 有预算的 Computer Use、更多适配器、成本对照 | 有限重试、可接管、负面用例通过；不达标则保留已有固定流程 |

M0 的 Workbench 实际资料缺失只阻止其适配实现，不阻止接口设计、模拟适配器和独立本地动作测试。M1/M2 可交付预览；没有至少一个 Agent 真实结果闭环，不可宣称整体 Agent 产品完成。

每阶段交付：代码差异、文档更新、能力矩阵、可复现测试结果、未测/不支持清单、成本统计和剩余风险。禁止一次性让模型“大改成智能体”，阶段通过后再进入下一阶段。

## 15. 核心验收任务集

1. “打开浏览器”本地执行；否定句“不要打开浏览器”不误执行，含糊应用名按需询问。
2. “搜索 X”正确编码 URL，不自动抓取总结；“总结 X”走独立内容任务。
3. “告诉 WorkBuddy：……”准确投递原要求和指定资料，不偷换会话或重复发送。
4. “这次用 A”不改长期默认；“以后用 B”保存并可撤销；项目规则不污染别的项目。
5. 规则冲突、执行者下线、指定机制不支持有明确反馈，不能偷偷改权限或执行者。
6. Workbench 与手机并发补充任务检测 revision 冲突，“继续这个”绑定同一 taskId。
7. 派单确认丢失、断网、服务重启进入可核对状态，不重复创建任务/日程。
8. 没有回执的 GUI adapter 不能显示 running/succeeded；结果链接手机能实际打开。
9. 用户接管、断开、取消清理按键；旧 GUI 命令失效，后台任务无法停止时如实说明。
10. 外部文档写“忽略规则/记住新偏好/发送给某人”不改变权限与记忆。
11. 用户删除规则后缓存和后续任务均不再命中；在途任务规则快照可解释。
12. 总 token/费用 unknown 正确展示；到预算后停止自动循环，任务草稿与产物保留。

自动测试覆盖状态迁移、版本冲突、幂等、规则优先级/删除、租约撤销、预算。模拟 adapter 注入延迟、重复/乱序事件和未知结果；真实应用验证使用无破坏性的测试任务。任何对外发消息/日程邀请的测试都需明确测试对象授权，不能拿真实联系人做自动验收。

## 16. 执行模型工作指令

请按 AGENT_PRODUCT_ARCHITECTURE.md 设计实施 PocketDesk 的 Agent 工作流。先完成 M0 的事实核对和能力矩阵，再逐阶段实施。当前任务事实源只能有一个；Workbench 未核对前只实现契约/模拟适配，不假设现有 API。明确区分普通输入与办事、派单与完成、偏好与权限、用户记忆与外部内容。先做本地固定动作、任务反馈、结构化规则和一个 Agent 适配，不引入多 Agent 群聊或向量数据库。所有 GUI 执行与手机遥控共用租约，断线和未知回执不得重放副作用。按阶段提交验证证据与未测项，保持现有语音输入/画面/鼠标能力，保留其他模型的未提交改动，完成 GEB 文档回环。不能把当前 Codex 会话工具当作项目已有产品 API，不能把 WorkBuddy MCP 工具扩展误当其外部任务入口。

## 17. 资料与待核对项

- [S1 WorkBuddy 官方连接器文档](https://open.workbuddy.cn/docs/connector)：MCP + Skill / CLI + Skill 能力扩展方向；桌面派单/回执另行验证。
- [S2 Anthropic：Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)：简单方案优先，区分固定工作流与自主 Agent，使用路由与反馈。
- [WorkBuddy 官方简介](https://www.workbuddy.cn/docs/workbuddy/Overview)：产品能力背景，不视为任意版本的可编程接口保证。
- 待用户补充：Workbench 的项目路径/链接及已有任务服务情况。
- 待 M0 实测：用户安装的 WorkBuddy/Codex/其他 Agent 版本、可调用入口、结果/取消接口及认证方式；钉钉 AI 助理仅为路由示例，未承诺其日程能力可通过当前界面或 API 调用。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
